[package]
name = "sebas-channels"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1.40", features = ["sync"] }
# 会话键编解码（key.rs 唯一实现，add-domain-layer 2.2）。
urlencoding = "2"
