## 1. Spec alignment

- [ ] 1.1 Apply the `state-store`「Runtime state boundaries」MODIFIED block (shutdown-only JSON + reserved placeholder table).

## 2. Validation

- [ ] 2.1 Confirm the C1 code fix + regression test (`writer_refuses_newer_database_at_startup`) is present (separate, already applied).
- [ ] 2.2 `openspec validate --changes` passes; archive checklist run.
