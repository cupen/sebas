# sebas P0 修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复体检发现的六条 P0 缺陷线（权限链三层、/cancel 语义、startup 超时、崩溃终态、token 刷新、双重 spawn 竞态），让 daemon 在真实环境跑通最小闭环。

**Architecture:** 不动现有架构。acp-claude 消除双重 session/new 并补生命周期保证；feishu 统一 ButtonCb 解析并新增 TokenManager；router 引入 Spawning 占位态。fake-claude 按"最小但保真"原则重设计作为测试地基。

**Tech Stack:** Rust workspace（sebas + router + feishu + acp-claude）、agent-client-protocol 2.0.0 SDK、tokio、serde_json。**不新增任何外部依赖**（token stub 用手撸 TCP listener；临时文件用 `std::env::temp_dir()`）。

**Spec:** `docs/superpowers/specs/2026-07-28-p0-fixes-design.md`（缺陷清单 D1-D8 见 §2）
**Beads:** sebas-8ig（组1）→ sebas-rfs（组2）→ sebas-jq0（组3）

## Global Constraints

- 全量回归：`cargo test --workspace` 必须保持全绿（sigterm 测试为 opt-in `--ignored`）。
- commit 规范：Conventional Commits 单行；3 个交付 commit（Task 5 / Task 10 / Task 12 末尾）+ Task 13 一个 docs commit；工作分支 `fix/p0-critical-chain`（从 `feat/initial-design` 切出）。
- fake-claude 的行为开关一律用 **argv flag**（`--journal` / `--hang-on-init` / `--delay-new-ms`），不用 env——env 是进程全局，cargo test 并行时会互相污染。这是对设计文档 §6.4（env 开关）的有意偏离，理由记录在此。
- 磁盘格式不变：`sessions.json` 的序列化形状与修复前完全一致（`Spawning` 永不落盘），无需迁移。
- 卡片流式模型、HelpText 实际发消息、媒体、reaction 状态机——本计划明确不碰（P1）。
- 权限流**永不超时**（spec §4.1）：不得给 permission responder 加任何超时。

---

## File Structure

| 文件 | 责任 | 涉及 Task |
|---|---|---|
| `tests/bin/fake-claude.rs` | 保真 ACP agent 桩（重写） | 1 |
| `acp-claude/src/manager.rs` | 单一 session/new、/cancel、启动超时、终态合成 | 2,3,6,7,8 |
| `acp-claude/src/session.rs` | `AcpEvent::Error` 增加 `terminal` 字段 | 7 |
| `acp-claude/tests/single_session_new.rs` | D1 回归测试（journal 断言） | 2 |
| `acp-claude/tests/cancel_keeps_session.rs` | D4 回归测试 | 3 |
| `acp-claude/tests/permission_roundtrip.rs` | 权限链端到端测试 | 5 |
| `acp-claude/tests/startup_timeout.rs` | D5 测试 | 6 |
| `acp-claude/tests/crash_terminal_event.rs` | D6 测试 | 8 |
| `feishu/src/events.rs` | `CardAction.decision` + V2 真实路径解析 | 4 |
| `feishu/tests/event_parse_test.rs` | ButtonCb 解析测试改写 | 4 |
| `feishu/src/client.rs` | `TokenManager` + 出站调用重试一次 | 10 |
| `feishu/tests/token_manager_test.rs` | TokenManager 测试（手撸 TCP stub） | 10 |
| `feishu/src/cards.rs` | 新增 `render_error_card` | 11 |
| `router/src/state.rs` | `MappingState` + `route_text`/`activate`/`fail_spawn` + dump 过滤 | 11 |
| `router/src/router.rs` | on_text 走 `route_text`、terminal Error 处理 | 9,11 |
| `router/tests/spawn_race_test.rs` | 占位态流转单测 | 11 |
| `src/run.rs` | 启动超时接线、TokenManager 接线、SpawnAcp 臂重构 | 6,10,11 |
| `tests/spawn_race_test.rs` | DELAY_NEW 竞态集成测试 | 12 |

---

## Task 1: fake-claude 保真重设计

**Files:**
- Modify: `tests/bin/fake-claude.rs`（整体重写）

**Interfaces:**
- Consumes: 无（workspace bin，独立 stdio JSON-RPC）
- Produces（后续 Task 依赖的行为契约）:
  - argv: `--journal <path>`（每条入站消息追加一行 `{"dir":"in","msg":<raw>}`）、`--hang-on-init`、`--delay-new-ms <n>`
  - `session/new` 每次返回唯一 id：`sess-1`、`sess-2`…（进程内计数器）
  - `session/prompt` 的 update 通知打的是 **params 里的 sessionId**；普通 prompt → `hello `/`world` + `end_turn`；`perm` → 发 `session/request_permission` 并阻塞回合直到收到 response；`crash` → 发一个 chunk 后 `exit(2)`
  - `session/cancel`：若该 session 有权限阻塞中的回合 → 以 `{"stopReason":"cancelled"}` 应答该 prompt 请求；否则忽略
  - 未知 request（带 id）→ `-32601`；未知 notification（无 id）→ 忽略

- [ ] **Step 1: 重写 fake-claude**

完整替换 `tests/bin/fake-claude.rs`：

```rust
//! Minimal-but-faithful ACP agent stand-in (design doc §6).
//!
//! Speaks ACP JSON-RPC on stdio without depending on the SDK.
//! Fidelity contract:
//! - `session/new` returns a GLOBALLY UNIQUE id per call (sess-1, sess-2, ...).
//! - Update notifications are tagged with the sessionId from the prompt's
//!   params (routing integrity), not a captured global.
//! - A turn ends with the prompt REQUEST's response carrying stopReason;
//!   updates are notifications (no id).
//! - Unknown requests (with id) get -32601; unknown notifications are ignored.
//! - `session/cancel` answers a permission-blocked turn with "cancelled";
//!   otherwise it is a no-op (accurate: no turn in flight).
//!
//! Behaviour switches are argv flags (NOT env vars — env is process-global
//! and races under `cargo test`'s parallelism):
//!   --journal <path>    append every inbound message as {"dir":"in","msg":...}
//!   --hang-on-init      never answer `initialize`
//!   --delay-new-ms <n>  sleep n ms before answering `session/new`

use serde_json::{Value, json};
use std::io::{self, BufRead, Write};

struct Flags {
    journal: Option<String>,
    hang_on_init: bool,
    delay_new_ms: u64,
}

fn parse_flags() -> Flags {
    let mut f = Flags {
        journal: None,
        hang_on_init: false,
        delay_new_ms: 0,
    };
    let mut it = std::env::args().skip(1);
    while let Some(a) = it.next() {
        match a.as_str() {
            "--journal" => f.journal = it.next(),
            "--hang-on-init" => f.hang_on_init = true,
            "--delay-new-ms" => {
                f.delay_new_ms = it.next().and_then(|v| v.parse().ok()).unwrap_or(0);
            }
            _ => {}
        }
    }
    f
}

fn main() {
    let flags = parse_flags();
    let mut journal = flags.journal.as_ref().map(|p| {
        std::fs::OpenOptions::new()
            .create(true)
            .append(true)
            .open(p)
            .expect("open journal")
    });
    let stdin = io::stdin();
    let stdout = io::stdout();
    let mut out = stdout.lock();

    let mut session_counter: u64 = 0;
    let mut rpc_counter: u64 = 0;
    // A turn blocked on a permission response: (perm_req_id, prompt_req_id, session_id).
    let mut pending_perm: Option<(Value, Value, String)> = None;

    for line in stdin.lock().lines() {
        let line = match line {
            Ok(l) => l,
            Err(_) => break,
        };
        let v: Value = match serde_json::from_str(&line) {
            Ok(v) => v,
            Err(_) => continue,
        };
        if let Some(j) = journal.as_mut() {
            let rec = json!({"dir": "in", "msg": v});
            let mut s = serde_json::to_string(&rec).unwrap_or_default();
            s.push('\n');
            let _ = j.write_all(s.as_bytes());
            let _ = j.flush();
        }

        let id = v.get("id").cloned();
        let method = v.get("method").and_then(|m| m.as_str()).unwrap_or("");

        if method.is_empty() {
            // A response (to our session/request_permission).
            if let (Some((perm_id, prompt_id, sid)), Some(rid)) = (&pending_perm, &id) {
                if perm_id == rid {
                    let (sid, prompt_id) = (sid.clone(), prompt_id.clone());
                    pending_perm = None;
                    send_chunk(&mut out, &sid, "perm done");
                    send(
                        &mut out,
                        json!({"jsonrpc":"2.0","id":prompt_id,"result":{"stopReason":"end_turn"}}),
                    );
                }
            }
            continue;
        }

        match method {
            "initialize" => {
                if flags.hang_on_init {
                    continue;
                }
                send(
                    &mut out,
                    json!({
                        "jsonrpc": "2.0",
                        "id": id,
                        "result": {
                            "protocolVersion": 1,
                            "agentCapabilities": {
                                "loadSession": false,
                                "promptCapabilities": {
                                    "image": false,
                                    "audio": false,
                                    "embeddedContext": false
                                },
                                "mcpCapabilities": {"http": false, "sse": false},
                                "sessionCapabilities": {}
                            },
                            "authMethods": [],
                            "agentInfo": {
                                "name": "fake-claude",
                                "title": "fake claude",
                                "version": "0.1.0"
                            }
                        }
                    }),
                );
            }
            "session/new" => {
                if flags.delay_new_ms > 0 {
                    std::thread::sleep(std::time::Duration::from_millis(flags.delay_new_ms));
                }
                session_counter += 1;
                let sid = format!("sess-{session_counter}");
                send(
                    &mut out,
                    json!({"jsonrpc":"2.0","id":id,"result":{"sessionId":sid}}),
                );
            }
            "session/prompt" => {
                let sid = v
                    .pointer("/params/sessionId")
                    .and_then(Value::as_str)
                    .unwrap_or("")
                    .to_string();
                let text = v
                    .pointer("/params/prompt/0/text")
                    .and_then(Value::as_str)
                    .unwrap_or("");
                let prompt_id = id.clone().unwrap_or(Value::Null);
                match text {
                    "crash" => {
                        send_chunk(&mut out, &sid, "boom");
                        std::process::exit(2);
                    }
                    "perm" => {
                        rpc_counter += 1;
                        let perm_id = json!(format!("perm-{rpc_counter}"));
                        send(
                            &mut out,
                            json!({
                                "jsonrpc": "2.0",
                                "id": perm_id,
                                "method": "session/request_permission",
                                "params": {
                                    "sessionId": sid,
                                    "toolCall": {
                                        "toolCallId": "tc-1",
                                        "title": "Bash",
                                        "rawInput": {"cmd": "rm -rf /"}
                                    },
                                    "options": [
                                        {"optionId": "allow_once", "name": "Allow once", "kind": "allow_once"},
                                        {"optionId": "allow_always", "name": "Allow session", "kind": "allow_always"},
                                        {"optionId": "reject_once", "name": "Deny", "kind": "reject_once"}
                                    ]
                                }
                            }),
                        );
                        pending_perm = Some((perm_id, prompt_id, sid));
                    }
                    _ => {
                        send_chunk(&mut out, &sid, "hello ");
                        send_chunk(&mut out, &sid, "world");
                        send(
                            &mut out,
                            json!({"jsonrpc":"2.0","id":prompt_id,"result":{"stopReason":"end_turn"}}),
                        );
                    }
                }
            }
            "session/cancel" => {
                let sid = v
                    .pointer("/params/sessionId")
                    .and_then(Value::as_str)
                    .unwrap_or("");
                if let Some((_, prompt_id, p_sid)) = &pending_perm {
                    if p_sid == sid {
                        let prompt_id = prompt_id.clone();
                        pending_perm = None;
                        send(
                            &mut out,
                            json!({"jsonrpc":"2.0","id":prompt_id,"result":{"stopReason":"cancelled"}}),
                        );
                    }
                }
                // No turn in flight -> ignore (accurate: cancel is a notification).
            }
            _ => {
                if let Some(req_id) = id {
                    send(
                        &mut out,
                        json!({"jsonrpc":"2.0","id":req_id,"error":{"code":-32601,"message":"method not found"}}),
                    );
                }
            }
        }
    }
}

fn send_chunk(out: &mut io::StdoutLock<'_>, sid: &str, text: &str) {
    send(
        out,
        json!({
            "jsonrpc": "2.0",
            "method": "session/update",
            "params": {
                "sessionId": sid,
                "update": {
                    "sessionUpdate": "agent_message_chunk",
                    "content": {"type": "text", "text": text}
                }
            }
        }),
    );
}

fn send(stdout: &mut io::StdoutLock<'_>, msg: Value) {
    let mut s = serde_json::to_string(&msg).unwrap_or_default();
    s.push('\n');
    let _ = stdout.write_all(s.as_bytes());
    let _ = stdout.flush();
}
```

- [ ] **Step 2: 构建并冒烟**

Run: `cargo build --bin fake-claude 2>&1 | tail -3`
Expected: `Compiling` + `Finished`，无 error。

冒烟（initialize → session/new ×2 → prompt → 期望唯一 id 与 hello/world）：

```bash
printf '%s\n' \
  '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}' \
  '{"jsonrpc":"2.0","id":2,"method":"session/new","params":{"cwd":"/tmp","mcpServers":[]}}' \
  '{"jsonrpc":"2.0","id":3,"method":"session/new","params":{"cwd":"/tmp","mcpServers":[]}}' \
  '{"jsonrpc":"2.0","id":4,"method":"session/prompt","params":{"sessionId":"sess-2","prompt":[{"type":"text","text":"hi"}]}}' \
  | ./target/debug/fake-claude | grep -o '"sessionId":"[^"]*"\|"text":"[^"]*"\|"stopReason":"[^"]*"'
```

Expected: `"sessionId":"sess-1"`、`"sessionId":"sess-2"`、`"text":"hello "`、`"text":"world"`、`"stopReason":"end_turn"`（update 打的是 sess-2）。

journal 冒烟：

```bash
printf '%s\n' \
  '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}' \
  '{"jsonrpc":"2.0","id":2,"method":"session/new","params":{"cwd":"/tmp/abc","mcpServers":[]}}' \
  | ./target/debug/fake-claude --journal /tmp/fc-journal.jsonl >/dev/null
cat /tmp/fc-journal.jsonl | grep -c '"dir":"in"'   # 期望 2
grep -o '"cwd":"/tmp/abc"' /tmp/fc-journal.jsonl    # 期望命中
```

- [ ] **Step 3: 跑现有 acp-claude 测试确认不失真**

Run: `cargo test -p acp-claude 2>&1 | grep -E "test result|running"`
Expected: 全部 `ok`（唯一 id + params 取 sessionId 的新 fake 下，旧测试不受影响）。

**此 Task 不单独 commit**（与组 1 其余 Task 一起落于 Task 5）。

---

## Task 2: D1 — 单一 session/new

**Files:**
- Test: `acp-claude/tests/single_session_new.rs`（新建）
- Modify: `acp-claude/src/manager.rs:272-298`（run_main 的 initialize/session-new 段 + import）

**Interfaces:**
- Consumes: Task 1 的 fake（`--journal`、唯一 id）；SDK `ActiveSession::session_id() -> &SessionId`（`session.rs:552`，`.0.to_string()` 取字符串）、`cx.build_session(cwd).block_task().start_session()`
- Produces: `create_session` 返回的 session id == 后续所有事件携带的 id == agent 侧唯一 session

- [ ] **Step 1: 写失败测试**

新建 `acp-claude/tests/single_session_new.rs`：

```rust
//! D1 regression: create_session must issue exactly ONE session/new, and the
//! id it returns must be the id the agent actually works on (updates carry
//! the same id). The pre-fix code sent session/new twice (manual + SDK
//! start_session), splitting the routing id from the working id.

use acp_claude::manager::SessionManager;
use acp_claude::session::{AcpCommand, AcpEvent};
use std::path::PathBuf;
use std::time::Duration;

fn fake() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .parent()
        .expect("workspace root")
        .join("target/debug/fake-claude")
}

fn journal_path(tag: &str) -> PathBuf {
    std::env::temp_dir().join(format!(
        "fc-journal-{}-{tag}-{}.jsonl",
        std::process::id(),
        std::time::SystemTime::now()
            .duration_since(std::time::UNIX_EPOCH)
            .map(|d| d.as_nanos())
            .unwrap_or(0)
    ))
}

fn journal_lines(path: &std::path::Path) -> Vec<serde_json::Value> {
    std::fs::read_to_string(path)
        .expect("journal exists")
        .lines()
        .filter(|l| !l.is_empty())
        .map(|l| serde_json::from_str(l).expect("journal line parses"))
        .collect()
}

fn count_method(lines: &[serde_json::Value], method: &str) -> usize {
    lines
        .iter()
        .filter(|l| l.pointer("/msg/method").and_then(|m| m.as_str()) == Some(method))
        .count()
}

#[tokio::test]
async fn exactly_one_session_new_and_routing_id_matches() {
    let journal = journal_path("single-new");
    let mgr = SessionManager::new();
    let id = mgr
        .create_session(
            fake().to_str().unwrap(),
            vec!["--journal".into(), journal.to_str().unwrap().into()],
            Some("/tmp".into()),
            "".into(),
        )
        .await
        .expect("spawn");
    mgr.send(
        &id,
        AcpCommand::CreateSession {
            session_id: id.clone(),
            prompt: "hi".into(),
        },
    )
    .await
    .expect("send prompt");

    // Drain until Finished.
    for _ in 0..10 {
        let evt = tokio::time::timeout(Duration::from_secs(2), mgr.next_event(&id))
            .await
            .expect("event timeout")
            .expect("stream open");
        if matches!(evt, AcpEvent::Finished { .. }) {
            break;
        }
    }

    let lines = journal_lines(&journal);
    assert_eq!(
        count_method(&lines, "session/new"),
        1,
        "expected exactly one session/new, journal: {lines:?}"
    );
    // cwd plumbing: the one session/new must carry the work_dir we passed.
    let cwd = lines
        .iter()
        .find(|l| l.pointer("/msg/method").and_then(|m| m.as_str()) == Some("session/new"))
        .and_then(|l| l.pointer("/msg/params/cwd"))
        .and_then(|c| c.as_str());
    assert_eq!(cwd, Some("/tmp"), "session/new cwd mismatch");
    // Routing integrity: every session/update the agent sent must be tagged
    // with the SAME id create_session returned.
    let update_sids: Vec<&str> = lines
        .iter()
        .filter(|l| l.pointer("/msg/method").and_then(|m| m.as_str()) == Some("session/update"))
        .filter_map(|l| l.pointer("/msg/params/sessionId").and_then(|s| s.as_str()))
        .collect();
    assert!(!update_sids.is_empty(), "agent sent no updates");
    assert!(
        update_sids.iter().all(|s| *s == id.as_str()),
        "updates tagged with a different session id than the routing id {id}: {update_sids:?}"
    );
    let _ = std::fs::remove_file(&journal);
}
```

- [ ] **Step 2: 确认测试失败**

Run: `cargo test -p acp-claude --test single_session_new -- --nocapture 2>&1 | tail -15`
Expected: FAIL——`session/new` 计数为 2（旧代码手动 + SDK 各发一次），或 update 的 sessionId 与路由 id 不一致。

- [ ] **Step 3: 修改 `acp-claude/src/manager.rs`**

import 段（第 7-12 行）删除 `NewSessionRequest`：

```rust
use agent_client_protocol::schema::v1::{
    CancelNotification, ClientCapabilities, Implementation, InitializeRequest, PermissionOptionId,
    RequestPermissionOutcome, RequestPermissionRequest, RequestPermissionResponse,
    SelectedPermissionOutcome, SessionId as SdkSessionId, SessionNotification, StopReason,
};
```

run_main 中（现 281-298 行）把手动 session/new + `build_session(".") 替换为：

```rust
    // 2) session/new — exactly once, via the SDK session builder. The
    //    SDK-issued session id is the single routing id for the whole
    //    pipeline (prompts, updates, permission requests, cancel).
    let mut session = cx
        .build_session(std::path::PathBuf::from(&cwd))
        .block_task()
        .start_session()
        .await?;
    let session_id = session.session_id().0.to_string();
    let _ = init_tx.send(session_id.clone());
```

（原 `// 2) session/new — capture the SDK session_id...` 整段与 `cx.build_session(std::path::PathBuf::from("."))` 的注释块一并替换；`cwd` 参数从 `"."` 修正为 `&cwd`，消除 work_dir 错配。）

- [ ] **Step 4: 确认测试通过 + 回归**

Run: `cargo test -p acp-claude --test single_session_new 2>&1 | grep "test result"`
Expected: `1 passed`。

Run: `cargo test -p acp-claude 2>&1 | grep "test result"`
Expected: 全部 ok（含 `no_duplicate_prompt`、`canned_test`）。

**此 Task 不单独 commit。**

---

## Task 3: D4 — /cancel 只取消回合，不杀 session

**Files:**
- Test: `acp-claude/tests/cancel_keeps_session.rs`（新建）
- Modify: `acp-claude/src/manager.rs:325-331`（Cancel 分支）

**Interfaces:**
- Consumes: Task 1 fake 的 `session/cancel` 语义、Task 2 修好的单一 session id
- Produces: `AcpCommand::Cancel` 发送后 session 仍可接受后续 prompt

- [ ] **Step 1: 写失败测试**

新建 `acp-claude/tests/cancel_keeps_session.rs`：

```rust
//! D4 regression: /cancel cancels the current TURN, not the session.
//! Pre-fix code broke the read loop after sending CancelNotification,
//! dropping the connection and SIGKILL-ing the child.

use acp_claude::manager::SessionManager;
use acp_claude::session::{AcpCommand, AcpEvent};
use std::path::PathBuf;
use std::time::Duration;

fn fake() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .parent()
        .expect("workspace root")
        .join("target/debug/fake-claude")
}

async fn drain_until_finished(mgr: &SessionManager, id: &str) {
    for _ in 0..10 {
        let evt = tokio::time::timeout(Duration::from_secs(2), mgr.next_event(id))
            .await
            .expect("event timeout")
            .expect("stream open");
        if matches!(evt, AcpEvent::Finished { .. }) {
            return;
        }
    }
    panic!("no Finished within 10 events");
}

#[tokio::test]
async fn session_survives_cancel() {
    let mgr = SessionManager::new();
    let id = mgr
        .create_session(fake().to_str().unwrap(), vec![], None, "".into())
        .await
        .expect("spawn");

    mgr.send(
        &id,
        AcpCommand::CreateSession {
            session_id: id.clone(),
            prompt: "hi".into(),
        },
    )
    .await
    .expect("initial prompt");
    drain_until_finished(&mgr, &id).await;

    // Cancel (no turn in flight — agent ignores it, which is fine).
    mgr.send(&id, AcpCommand::Cancel {
        session_id: id.clone(),
    })
    .await
    .expect("cancel");

    // The session must still accept and answer a follow-up prompt.
    mgr.send(
        &id,
        AcpCommand::ContinueSession {
            session_id: id.clone(),
            prompt: "again".into(),
        },
    )
    .await
    .expect("follow-up prompt after cancel");
    drain_until_finished(&mgr, &id).await;
}
```

- [ ] **Step 2: 确认失败**

Run: `cargo test -p acp-claude --test cancel_keeps_session -- --nocapture 2>&1 | tail -8`
Expected: FAIL——旧代码 cancel 后 break，follow-up prompt 石沉大海（event timeout / channel closed）。

- [ ] **Step 3: 修改 Cancel 分支**

`acp-claude/src/manager.rs` 中：

```rust
                    Some(AcpCommand::Cancel { .. }) => {
                        // Cancel the current TURN only; the session stays
                        // alive. The agent answers with StopReason::Cancelled
                        // which the read loop maps to a turn-level Finished.
                        let _ = cx.send_notification(CancelNotification::new(
                            SdkSessionId::new(session_id.clone()),
                        ));
                    }
```

（删除原有的 `evt_tx.send(AcpEvent::Finished ...)` 与 `break`。）

同时把 `kill` 路径（`cancel_rx` 臂，约 316-321 行）的注释更新为说明 kill 仍终止 session——代码不变。

- [ ] **Step 4: 确认通过 + 回归**

Run: `cargo test -p acp-claude 2>&1 | grep "test result"`
Expected: 全部 ok。

**此 Task 不单独 commit。**

---

## Task 4: D2/D3 — ButtonCb 解析归一

**Files:**
- Modify: `feishu/src/events.rs:48-53`（CardAction）、`feishu/src/events.rs:82-114`（card.action.trigger 分支）
- Modify: `router/src/router.rs:217-226`（on_button decision 读取）
- Test: `feishu/tests/event_parse_test.rs:79-156`（改写两个 ButtonCb 测试）
- Test: `router/tests/permission_test.rs:71-96`（CardAction 字面量加 `decision` 字段）

**Interfaces:**
- Consumes: 无
- Produces: `CardAction { session_id: String, request_id: Option<String>, decision: Option<String>, value: serde_json::Value }`；router 只消费 `action.decision`（缺省/未知 → Deny）

- [ ] **Step 1: 改写失败测试（feishu）**

`feishu/tests/event_parse_test.rs` 的 `parses_card_action_trigger_to_buttoncb` 改为 V2 真实形状（`event.action.value.*`）：

```rust
#[test]
fn parses_card_action_trigger_to_buttoncb() {
    // Real Feishu V2 card callback: the button's behaviors[].value object is
    // delivered back at event.action.value. This mirrors what
    // cards.rs::render_permission_card writes.
    let raw = serde_json::json!({
        "schema": "2.0",
        "header": { "event_type": "card.action.trigger", "tenant_key": "tk" },
        "event": {
            "chat_id": "oc_perm",
            "chat_type": "p2p",
            "thread_id": "omt_thread",
            "action": {
                "value": {
                    "session_id": "sess_42",
                    "request_id": "req_42",
                    "decision": "allow_once"
                }
            }
        }
    });
    let env: FeishuEnvelope = serde_json::from_value(raw).unwrap();
    let evt = env.into_event("").expect("should parse");
    let FeishuIn::ButtonCb { key, action } = evt else {
        panic!("expected ButtonCb");
    };
    assert_eq!(
        key,
        SessionKey {
            chat_id: "oc_perm".to_string(),
            thread_id: Some("omt_thread".to_string()),
        }
    );
    assert_eq!(action.session_id, "sess_42");
    assert_eq!(action.request_id.as_deref(), Some("req_42"));
    assert_eq!(action.decision.as_deref(), Some("allow_once"));
}

/// Legacy flat layout (/action/session_id + /action/value/decision split)
/// still parses — tolerance against doc-vs-reality drift.
#[test]
fn parses_card_action_trigger_legacy_flat_layout() {
    let raw = serde_json::json!({
        "schema": "2.0",
        "header": { "event_type": "card.action.trigger" },
        "event": {
            "chat_id": "oc_x",
            "action": {
                "session_id": "sess_leg",
                "request_id": "req_leg",
                "value": { "decision": "deny" }
            }
        }
    });
    let env: FeishuEnvelope = serde_json::from_value(raw).unwrap();
    let FeishuIn::ButtonCb { action, .. } = env.into_event("").expect("parses") else {
        panic!("expected ButtonCb");
    };
    assert_eq!(action.session_id, "sess_leg");
    assert_eq!(action.request_id.as_deref(), Some("req_leg"));
    assert_eq!(action.decision.as_deref(), Some("deny"));
}
```

`card_action_trigger_without_session_id_defaults_empty` 保留，但断言从 `value.pointer(...)` 改为字段：

```rust
    assert_eq!(action.session_id, "");
    assert_eq!(action.decision.as_deref(), Some("allow_once"));
```

- [ ] **Step 2: 确认失败**

Run: `cargo test -p feishu --test event_parse_test 2>&1 | tail -8`
Expected: FAIL（编译错误：`CardAction` 无 `decision` 字段）。

- [ ] **Step 3: 修改 events.rs**

CardAction（48-53 行）：

```rust
#[derive(Debug, Clone)]
pub struct CardAction {
    pub session_id: String,
    pub request_id: Option<String>,
    /// Parsed at the events layer (which owns payload shape); the router
    /// consumes only this field for the allow/deny policy.
    pub decision: Option<String>,
    /// Raw event object, retained for debugging/future fields.
    pub value: serde_json::Value,
}
```

into_event 的 card.action.trigger 分支（82-114 行），将 session_id/request_id 两段读取替换为：

```rust
        if self.header.event_type == "card.action.trigger" {
            let chat_id = self
                .event
                .pointer("/chat_id")
                .or_else(|| self.event.pointer("/message/chat_id"))
                .and_then(serde_json::Value::as_str)?
                .to_owned();
            let thread_id = self
                .event
                .pointer("/thread_id")
                .or_else(|| self.event.pointer("/message/thread_id"))
                .and_then(serde_json::Value::as_str)
                .map(str::to_owned);
            // Primary: the V2 button behaviors[].value round-trip location.
            // Fallback: the legacy flat layout (tolerance against payload drift).
            let pick = |primary: &str, legacy: &str| {
                self.event
                    .pointer(primary)
                    .or_else(|| self.event.pointer(legacy))
                    .and_then(serde_json::Value::as_str)
                    .map(str::to_owned)
            };
            let session_id =
                pick("/action/value/session_id", "/action/session_id").unwrap_or_default();
            let request_id = pick("/action/value/request_id", "/action/request_id");
            let decision = pick("/action/value/decision", "/action/decision");
            return Some(FeishuIn::ButtonCb {
                key: SessionKey { chat_id, thread_id },
                action: CardAction {
                    session_id,
                    request_id,
                    decision,
                    value: self.event,
                },
            });
        }
```

- [ ] **Step 4: 修改 router 的 on_button（D3）**

`router/src/router.rs:217-226` 替换为：

```rust
        let decision = match action.decision.as_deref() {
            Some("allow_once") => Decision::AllowOnce,
            Some("allow_session") => Decision::AllowSession,
            // Fail closed: unknown or missing decision is a deny.
            _ => Decision::Deny,
        };
```

- [ ] **Step 5: 修 router 测试的 CardAction 字面量**

`router/tests/permission_test.rs` 两处 `CardAction { ... }` 字面量（`button_callback_emits_permission_reply` 与 `button_callback_on_dead_session_emits_help_card`）补字段：

```rust
    let action = CardAction {
        session_id: "s1".into(),
        request_id: Some("r1".into()),
        decision: Some("allow_once".into()),
        value: serde_json::json!({ "decision": "allow_once" }),
    };
```

（第二个测试同样补 `decision: Some("allow_once".into())`。）

新增一个 fail-closed 测试到 `router/tests/permission_test.rs` 末尾：

```rust
#[tokio::test]
async fn button_callback_unknown_decision_defaults_to_deny() {
    let map = SessionMap::new();
    let key = SessionKey {
        chat_id: "oc_x".into(),
        thread_id: None,
    };
    map.insert(
        key.clone(),
        Mapping {
            session_id: "s1".into(),
            last_active_unix: 1,
        },
    )
    .await
    .unwrap();
    let (router, mut out_rx) = RouterHandle::new(map.clone());
    let action = CardAction {
        session_id: "s1".into(),
        request_id: Some("r1".into()),
        decision: None, // malformed payload -> fail closed
        value: serde_json::json!({}),
    };
    router.dispatch(FeishuIn::ButtonCb { key, action }).await;
    let out = tokio::time::timeout(Duration::from_millis(200), out_rx.recv())
        .await
        .unwrap()
        .unwrap();
    match out {
        Out::SendAcp {
            cmd: AcpCommand::PermissionReply { decision, .. },
            ..
        } => assert!(matches!(decision, Decision::Deny)),
        other => panic!("expected SendAcp PermissionReply, got {other:?}"),
    }
}
```

- [ ] **Step 6: 确认通过 + 回归**

Run: `cargo test -p feishu -p router 2>&1 | grep "test result"`
Expected: 全部 ok。

**此 Task 不单独 commit。**

---

## Task 5: 组 1 集成 — 权限链端到端 + commit 1

**Files:**
- Test: `acp-claude/tests/permission_roundtrip.rs`（新建）

**Interfaces:**
- Consumes: Task 1 fake 的 `perm` 模式、Task 2 的单一 id、Task 4 的 decision 解析（router 侧已测，此测试覆盖 acp 侧 responder 链路）
- Produces: 权限请求事件携带的 session_id 与路由 id 一致；`PermissionReply` 能送达 agent 并被 journal 记录

- [ ] **Step 1: 写失败测试（在旧代码上必败，此处用于验证全链修复）**

新建 `acp-claude/tests/permission_roundtrip.rs`：

```rust
//! End-to-end permission chain: prompt "perm" -> agent issues
//! session/request_permission -> AcpEvent::PermissionRequest (carrying the
//! ROUTING session id, not a split second id) -> PermissionReply reaches the
//! agent -> turn completes. Journal asserts the protocol facts.

use acp_claude::manager::SessionManager;
use acp_claude::session::{AcpCommand, AcpEvent, Decision};
use std::path::PathBuf;
use std::time::Duration;

fn fake() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .parent()
        .expect("workspace root")
        .join("target/debug/fake-claude")
}

#[tokio::test]
async fn permission_round_trip() {
    let journal = std::env::temp_dir().join(format!(
        "fc-journal-{}-perm-{}.jsonl",
        std::process::id(),
        std::time::SystemTime::now()
            .duration_since(std::time::UNIX_EPOCH)
            .map(|d| d.as_nanos())
            .unwrap_or(0)
    ));
    let mgr = SessionManager::new();
    let id = mgr
        .create_session(
            fake().to_str().unwrap(),
            vec!["--journal".into(), journal.to_str().unwrap().into()],
            None,
            "".into(),
        )
        .await
        .expect("spawn");
    mgr.send(
        &id,
        AcpCommand::CreateSession {
            session_id: id.clone(),
            prompt: "perm".into(),
        },
    )
    .await
    .expect("prompt perm");

    // Expect PermissionRequest carrying the routing id.
    let mut request_id = None;
    for _ in 0..10 {
        let evt = tokio::time::timeout(Duration::from_secs(2), mgr.next_event(&id))
            .await
            .expect("event timeout")
            .expect("stream open");
        match evt {
            AcpEvent::PermissionRequest {
                session_id,
                request_id: rid,
                tool_name,
                ..
            } => {
                assert_eq!(
                    session_id, id,
                    "permission event session id must equal the routing id"
                );
                assert_eq!(tool_name, "Bash");
                request_id = Some(rid);
                break;
            }
            _ => continue,
        }
    }
    let request_id = request_id.expect("no PermissionRequest within 10 events");

    // Reply allow_once; the turn must then complete.
    mgr.send(
        &id,
        AcpCommand::PermissionReply {
            session_id: id.clone(),
            request_id,
            decision: Decision::AllowOnce,
        },
    )
    .await
    .expect("reply");

    let mut finished = false;
    for _ in 0..10 {
        let evt = tokio::time::timeout(Duration::from_secs(2), mgr.next_event(&id))
            .await
            .expect("event timeout")
            .expect("stream open");
        if matches!(evt, AcpEvent::Finished { .. }) {
            finished = true;
            break;
        }
    }
    assert!(finished, "turn did not complete after permission reply");

    // Journal: the permission RESPONSE we sent selected allow_once.
    let raw = std::fs::read_to_string(&journal).expect("journal exists");
    let resp_line = raw
        .lines()
        .find(|l| l.contains("\"perm-1\"") && l.contains("\"result\""))
        .expect("permission response in journal");
    assert!(
        resp_line.contains("allow_once"),
        "expected allow_once in permission response: {resp_line}"
    );
    let _ = std::fs::remove_file(&journal);
}
```

- [ ] **Step 2: 运行并确认通过（组 1 修复已全部就位）**

Run: `cargo test -p acp-claude --test permission_roundtrip -- --nocapture 2>&1 | tail -6`
Expected: PASS。（若在 Task 2 之前的代码上跑，PermissionRequest 携带的是第二个 session id，断言 `session_id == id` 失败；权限等待永久挂起。）

- [ ] **Step 3: 全量回归**

Run: `cargo test --workspace 2>&1 | grep -E "test result|error"`
Expected: 全部 ok，无 error。

- [ ] **Step 4: Commit 1（组 1）**

```bash
git checkout -b fix/p0-critical-chain 2>/dev/null || git checkout fix/p0-critical-chain
git add tests/bin/fake-claude.rs acp-claude/src/manager.rs \
  acp-claude/tests/single_session_new.rs acp-claude/tests/cancel_keeps_session.rs \
  acp-claude/tests/permission_roundtrip.rs \
  feishu/src/events.rs feishu/tests/event_parse_test.rs \
  router/src/router.rs router/tests/permission_test.rs
git commit -m "fix(acp-claude,feishu,router): repair permission chain (single session/new, ButtonCb parsing, /cancel keeps session)"
```

```bash
bd update sebas-8ig --status=in_progress 2>/dev/null; bd close sebas-8ig --reason="组1完成：单一 session/new + ButtonCb V2 解析 + /cancel 不杀 session + fake-claude 保真重设计，测试全绿"
```

---

## Task 6: D5 — startup_timeout 接线

**Files:**
- Modify: `acp-claude/src/manager.rs:22-37`（struct/new）、`acp-claude/src/manager.rs:59-116`（create_session 等待段）
- Modify: `src/run.rs:33`（SessionManager::new 调用点）
- Modify: `acp-claude/tests/canned_test.rs:8`、`acp-claude/tests/lifecycle_test.rs:13,26`、`acp-claude/tests/no_duplicate_prompt.rs:18`、`acp-claude/tests/single_session_new.rs`、`acp-claude/tests/cancel_keeps_session.rs`、`acp-claude/tests/permission_roundtrip.rs`（`SessionManager::new()` → `SessionManager::new(Duration::from_secs(30))`）
- Test: `acp-claude/tests/startup_timeout.rs`（新建）

**Interfaces:**
- Consumes: `cfg.acp.claude.startup_timeout_secs`（config.rs:68-69，默认 30）
- Produces: `SessionManager::new(startup_timeout: std::time::Duration)`；超时路径触发半成品回收（abort session 任务 → SDK ChildGuard SIGKILL 子进程）

- [ ] **Step 1: 写失败测试**

新建 `acp-claude/tests/startup_timeout.rs`：

```rust
//! D5: create_session must give up within startup_timeout when the agent
//! never answers `initialize` — and tear down the half-spawned child instead
//! of leaking it.

use acp_claude::manager::SessionManager;
use std::path::PathBuf;
use std::time::{Duration, Instant};

fn fake() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .parent()
        .expect("workspace root")
        .join("target/debug/fake-claude")
}

#[tokio::test]
async fn hanging_agent_times_out_and_is_reaped() {
    let mgr = SessionManager::new(Duration::from_millis(400));
    let t0 = Instant::now();
    let res = mgr
        .create_session(
            fake().to_str().unwrap(),
            vec!["--hang-on-init".into()],
            None,
            "".into(),
        )
        .await;
    let elapsed = t0.elapsed();
    let err = res.expect_err("hanging agent must not yield a session");
    assert!(
        err.to_string().contains("timed out"),
        "error should mention timeout: {err}"
    );
    assert!(
        elapsed < Duration::from_secs(3),
        "timeout took too long: {elapsed:?}"
    );
    // Give the teardown a moment, then assert no fake-claude child lingers.
    tokio::time::sleep(Duration::from_millis(300)).await;
    let out = std::process::Command::new("pgrep")
        .args(["-f", "fake-claude --hang-on-init"])
        .output()
        .expect("pgrep");
    assert!(
        out.stdout.is_empty(),
        "fake-claude child leaked after timeout: {}",
        String::from_utf8_lossy(&out.stdout)
    );
}
```

- [ ] **Step 2: 确认失败（编译错误）**

Run: `cargo test -p acp-claude --test startup_timeout 2>&1 | tail -4`
Expected: 编译 FAIL——`SessionManager::new` 不接受参数。

- [ ] **Step 3: 修改 SessionManager**

`acp-claude/src/manager.rs`：

```rust
pub struct SessionManager {
    inner: Arc<Mutex<HashMap<String, SessionMeta>>>,
    startup_timeout: std::time::Duration,
}

impl Default for SessionManager {
    fn default() -> Self {
        Self::new(std::time::Duration::from_secs(30))
    }
}

impl SessionManager {
    pub fn new(startup_timeout: std::time::Duration) -> Self {
        Self {
            inner: Arc::new(Mutex::new(HashMap::new())),
            startup_timeout,
        }
    }
```

create_session 的 spawn + 等待段（现 79-98 行）改为持有 JoinHandle 并包超时：

```rust
        let join = tokio::spawn(async move {
            let result = run_session(
                agent,
                cwd,
                cmd_rx,
                evt_tx,
                cancel_rx,
                init_tx,
                pending_responders_for_cb,
                evt_tx_for_cb,
            )
            .await;
            if let Err(e) = result {
                tracing::error!(?e, "acp session task ended with error");
            }
        });

        let session_id = match tokio::time::timeout(self.startup_timeout, init_rx).await {
            Ok(Ok(sid)) => sid,
            Ok(Err(_)) => anyhow::bail!("acp session closed before session/new"),
            Err(_) => {
                // Tear down the half-spawned session: aborting the task drops
                // the SDK connection, whose ChildGuard SIGKILLs the process
                // group. Sending cancel would not help — run_main is stuck in
                // the handshake and never reaches its select loop.
                join.abort();
                anyhow::bail!(
                    "acp session start timed out after {:?}",
                    self.startup_timeout
                );
            }
        };
```

注意：`Ok(Err(_))` 臂（init_tx 被 drop = 握手失败）也可补 `join.abort()` 外的对称处理——实际上 run_session 返回 Err 后任务自然结束，无需 abort。

- [ ] **Step 4: 更新调用点**

`src/run.rs:33`：

```rust
    let mgr = Arc::new(SessionManager::new(std::time::Duration::from_secs(
        cfg.acp.claude.startup_timeout_secs,
    )));
```

各测试文件 `SessionManager::new()` → `SessionManager::new(Duration::from_secs(30))`（canned/lifecycle/no_duplicate_prompt/single_session_new/cancel_keeps_session/permission_roundtrip；lifecycle_test.rs 需补 `use std::time::Duration;`，canned/no_duplicate_prompt 已有）。

- [ ] **Step 5: 确认通过 + 回归**

Run: `cargo test -p acp-claude --test startup_timeout -- --nocapture 2>&1 | tail -4`
Expected: PASS。

Run: `cargo test --workspace 2>&1 | grep -E "test result|error"`
Expected: 全部 ok。

**此 Task 不单独 commit。**

---

## Task 7: D6a — `AcpEvent::Error` 增加 `terminal` 字段

**Files:**
- Modify: `acp-claude/src/session.rs:102-105`（Error 变体）
- Modify: `acp-claude/src/manager.rs`（Error 构造点：现 340、351、381、398 行 + `translate_stop_reason` 438 行）
- Test: `acp-claude/tests/parse_test.rs`（追加一条 serde 兼容断言）

**Interfaces:**
- Consumes: 无
- Produces: `AcpEvent::Error { session_id: String, message: String, terminal: bool }`（`#[serde(default)]`——旧 fixture/旧序列化数据缺字段时 = `false`）

- [ ] **Step 1: 先加失败断言**

`acp-claude/tests/parse_test.rs` 末尾追加：

```rust
#[test]
fn error_terminal_defaults_to_false_and_round_trips() {
    // Legacy shape (no terminal field) still parses, defaulting to false.
    let legacy = r#"{"type":"error","session_id":"s1","message":"boom"}"#;
    let evt: AcpEvent = serde_json::from_str(legacy).expect("legacy parses");
    match evt {
        AcpEvent::Error {
            session_id,
            message,
            terminal,
        } => {
            assert_eq!(session_id, "s1");
            assert_eq!(message, "boom");
            assert!(!terminal);
        }
        other => panic!("expected Error, got {other:?}"),
    }
    // New shape round-trips.
    let evt = AcpEvent::Error {
        session_id: "s2".into(),
        message: "dead".into(),
        terminal: true,
    };
    let s = serde_json::to_string(&evt).unwrap();
    assert!(s.contains("\"terminal\":true"));
    let back: AcpEvent = serde_json::from_str(&s).unwrap();
    assert!(matches!(back, AcpEvent::Error { terminal: true, .. }));
}
```

- [ ] **Step 2: 确认失败**

Run: `cargo test -p acp-claude --test parse_test 2>&1 | tail -4`
Expected: 编译 FAIL（Error 变体无 `terminal` 字段）。

- [ ] **Step 3: 修改 session.rs + manager.rs 构造点**

`acp-claude/src/session.rs`：

```rust
    Error {
        session_id: String,
        message: String,
        /// True when the session is unrecoverably dead (process exit,
        /// transport failure) — the router removes the mapping and shows ❌.
        /// `#[serde(default)]` keeps legacy fixtures/deserialization working.
        #[serde(default)]
        terminal: bool,
    },
```

`acp-claude/src/manager.rs` 五个构造点全部补 `terminal: true`（这些都是 exit-bound 路径：send_prompt 失败 ×2、Refusal ×2 处含 translate_stop_reason、transport Err）：

```rust
AcpEvent::Error {
    session_id: session_id.clone(),
    message: "session/prompt failed".into(),
    terminal: true,
}
```

`translate_stop_reason` 的 Refusal 臂：

```rust
        StopReason::Refusal => AcpEvent::Error {
            session_id: session_id.to_string(),
            message: "agent refused".into(),
            terminal: true,
        },
```

同时 grep 确认无遗漏构造点：

Run: `grep -rn "AcpEvent::Error {" --include="*.rs" acp-claude/ router/ feishu/ src/ tests/ | grep -v "terminal" | grep -v tests/fixtures`
Expected: 无输出（除模式匹配臂 `AcpEvent::Error { .. }` / `AcpEvent::Error { session_id, .. }` 等带 `..` 的形式——它们兼容新字段，无需改）。

- [ ] **Step 4: 确认通过 + 回归**

Run: `cargo test -p acp-claude 2>&1 | grep "test result"`
Expected: 全部 ok（含新断言与旧 fixture 的 7 事件解析）。

**此 Task 不单独 commit。**

---

## Task 8: D6b — 崩溃终态事件合成 + manager 表清理

**Files:**
- Modify: `acp-claude/src/session.rs:33-36`（SessionMeta 加 `expected_exit`）
- Modify: `acp-claude/src/manager.rs`（create_session 的 spawn 段、run_session/run_main 签名、kill/kill_all）
- Test: `acp-claude/tests/crash_terminal_event.rs`（新建）

**Interfaces:**
- Consumes: Task 1 fake 的 `crash` prompt、Task 7 的 `terminal` 字段
- Produces:
  - `run_session(..., terminal_sent: Arc<AtomicBool>, sid_slot: Arc<Mutex<Option<String>>>)`（新增两个参数，置于参数表末尾）
  - `SessionMeta.expected_exit: Arc<AtomicBool>`
  - 不变量：session 非正常死亡 → 恰好一个 `Error{terminal:true}` 上行 + manager 表无残留 + 事件通道随后关闭

- [ ] **Step 1: 写失败测试**

新建 `acp-claude/tests/crash_terminal_event.rs`：

```rust
//! D6: a crashing agent must surface exactly one Error{terminal:true},
//! be removed from the manager table, and close its event stream.

use acp_claude::manager::SessionManager;
use acp_claude::session::{AcpCommand, AcpEvent};
use std::path::PathBuf;
use std::time::Duration;

fn fake() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .parent()
        .expect("workspace root")
        .join("target/debug/fake-claude")
}

#[tokio::test]
async fn crash_yields_terminal_error_and_cleanup() {
    let mgr = SessionManager::new(Duration::from_secs(30));
    let id = mgr
        .create_session(fake().to_str().unwrap(), vec![], None, "".into())
        .await
        .expect("spawn");
    mgr.send(
        &id,
        AcpCommand::CreateSession {
            session_id: id.clone(),
            prompt: "crash".into(),
        },
    )
    .await
    .expect("crash prompt");

    // Expect: one chunk ("boom"), then exactly one Error{terminal:true},
    // then the stream closes.
    let mut saw_terminal = false;
    for _ in 0..10 {
        let evt = tokio::time::timeout(Duration::from_secs(3), mgr.next_event(&id))
            .await
            .expect("event timeout — no terminal event synthesized");
        match evt {
            None => break, // stream closed
            Some(AcpEvent::Error {
                session_id,
                terminal: true,
                ..
            }) => {
                assert_eq!(session_id, id);
                assert!(!saw_terminal, "duplicate terminal event");
                saw_terminal = true;
            }
            Some(AcpEvent::Error { terminal: false, .. }) => {
                panic!("non-terminal Error from a crash path");
            }
            Some(_) => continue,
        }
    }
    assert!(saw_terminal, "no terminal Error received");

    // Table cleanup: further sends fail fast.
    let res = mgr
        .send(
            &id,
            AcpCommand::ContinueSession {
                session_id: id.clone(),
                prompt: "anyone there?".into(),
            },
        )
        .await;
    assert!(res.is_err(), "dead session must reject sends");
}

#[tokio::test]
async fn explicit_kill_produces_no_terminal_error() {
    let mgr = SessionManager::new(Duration::from_secs(30));
    let id = mgr
        .create_session(fake().to_str().unwrap(), vec![], None, "".into())
        .await
        .expect("spawn");
    mgr.kill(&id).await;
    // Drain whatever remains; nothing terminal may be synthesized for an
    // explicit kill, and the stream must close promptly.
    let deadline = Instant::now() + Duration::from_secs(3);
    loop {
        match tokio::time::timeout(Duration::from_millis(300), mgr.next_event(&id)).await {
            Ok(Some(AcpEvent::Error { terminal: true, .. })) => {
                panic!("terminal Error synthesized for explicit kill")
            }
            Ok(Some(_)) => continue,
            Ok(None) => break,
            Err(_) => {
                assert!(Instant::now() < deadline, "stream did not close after kill");
                break;
            }
        }
    }
}
```

文件头部需要 `use std::time::Instant;`（第二个测试用到）。

注意：`explicit_kill_produces_no_terminal_error` 中 `mgr.next_event(&id)` 在 kill 后返回 None（entry 已移除，`next_event` 查表 miss → None），这恰好满足"stream closed"分支。

- [ ] **Step 2: 确认失败**

Run: `cargo test -p acp-claude --test crash_terminal_event -- --nocapture 2>&1 | tail -8`
Expected: FAIL——crash 路径无终态事件（event timeout），且死 session 仍占表（`send` 返回 Ok 或 channel 错误而非 "unknown session"）。

- [ ] **Step 3: 实现标志 + 合成 + 清理**

`acp-claude/src/session.rs`：

```rust
pub struct SessionMeta {
    pub session_id: String,
    pub handle: AcpSessionHandle,
    /// Set by kill()/kill_all() before signalling shutdown, so the wrapper
    /// task does not synthesize a crash event for an explicit kill.
    pub expected_exit: std::sync::Arc<std::sync::atomic::AtomicBool>,
}
```

`acp-claude/src/manager.rs`（多处）：

create_session 内，`tokio::spawn` 之前创建共享状态：

```rust
        let expected_exit = Arc::new(std::sync::atomic::AtomicBool::new(false));
        let terminal_sent = Arc::new(std::sync::atomic::AtomicBool::new(false));
        let sid_slot: Arc<Mutex<Option<String>>> = Arc::new(Mutex::new(None));
```

spawn 段改为（替换 Task 6 的 spawn 块——保留 `join` 以供超时 abort）：

```rust
        let join = tokio::spawn({
            let expected_exit = expected_exit.clone();
            let sid_slot = sid_slot.clone();
            let inner = self.inner.clone();
            let evt_tx_keep = evt_tx.clone();
            let terminal_sent_for_run = terminal_sent.clone();
            async move {
                let result = run_session(
                    agent,
                    cwd,
                    cmd_rx,
                    evt_tx,
                    cancel_rx,
                    init_tx,
                    pending_responders_for_cb,
                    evt_tx_for_cb,
                    terminal_sent_for_run,
                    sid_slot.clone(),
                )
                .await;
                if let Err(e) = &result {
                    tracing::error!(?e, "acp session task ended with error");
                }
                // Terminal-event guarantee (design §4.2): a session that dies
                // without an explicit kill surfaces exactly one
                // Error{terminal:true}; then the table entry is dropped so
                // send() fails fast and all senders close the stream.
                let sid = sid_slot.lock().await.clone();
                if let Some(session_id) = sid {
                    if !expected_exit.load(std::sync::atomic::Ordering::SeqCst)
                        && !terminal_sent.load(std::sync::atomic::Ordering::SeqCst)
                    {
                        let _ = evt_tx_keep
                            .send(AcpEvent::Error {
                                session_id: session_id.clone(),
                                message: "agent process exited".into(),
                                terminal: true,
                            })
                            .await;
                    }
                    inner.lock().await.remove(&session_id);
                }
            }
        });
```

`terminal_sent` 本体要在 run_session 内标记——把 `terminal_sent`（不是 `terminal_sent_for_run`）传入会move冲突，上面的克隆写法已处理：create_session 持有 `terminal_sent` 不再使用，仅 run 路径使用。

run_session / run_main 签名末尾追加：

```rust
async fn run_session(
    agent: AcpAgent,
    cwd: String,
    cmd_rx: mpsc::Receiver<AcpCommand>,
    evt_tx: mpsc::Sender<AcpEvent>,
    cancel_rx: oneshot::Receiver<()>,
    init_tx: oneshot::Sender<String>,
    pending_responders: Arc<Mutex<HashMap<String, ResponderSlot>>>,
    evt_tx_for_cb: mpsc::Sender<AcpEvent>,
    terminal_sent: Arc<std::sync::atomic::AtomicBool>,
    sid_slot: Arc<Mutex<Option<String>>>,
) -> Result<(), agent_client_protocol::Error> {
    Client
        .builder()
        .on_receive_request(/* 不变 */)
        .connect_with(agent, move |cx: ConnectionTo<Agent>| async move {
            run_main(cx, cwd, init_tx, evt_tx, cmd_rx, cancel_rx, terminal_sent, sid_slot).await
        })
        .await
}
```

run_main 内：

```rust
    let session_id = session.session_id().0.to_string();
    *sid_slot.lock().await = Some(session_id.clone());
    let _ = init_tx.send(session_id.clone());
```

封装一个发送辅助（run_main 内、select 循环之前定义），所有 exit-bound 发送统一置位：

```rust
    macro_rules! send_terminal {
        ($evt:expr) => {{
            terminal_sent.store(true, std::sync::atomic::Ordering::SeqCst);
            let _ = evt_tx.send($evt).await;
        }};
    }
```

各 exit-bound 发送点替换：
- cancel_rx 臂（kill 路径）：`send_terminal!(AcpEvent::Finished { session_id: session_id.clone() });` 然后 `break;`
- send_prompt 失败 ×2：`send_terminal!(AcpEvent::Error { session_id: session_id.clone(), message: "session/prompt failed".into(), terminal: true });` 然后 `break;`
- Refusal：`send_terminal!(AcpEvent::Error { session_id: session_id.clone(), message: "agent refused".into(), terminal: true });` 然后 `should_exit = true;`
- transport Err：`send_terminal!(AcpEvent::Error { session_id: session_id.clone(), message: format!("acp transport error: {e}"), terminal: true });` 然后 `break;`

kill / kill_all 置位 expected_exit：

```rust
    pub async fn kill(&self, session_id: &str) {
        if let Some(meta) = self.inner.lock().await.remove(session_id) {
            meta.expected_exit
                .store(true, std::sync::atomic::Ordering::SeqCst);
            if let Some(tx) = meta.handle.cancel_tx {
                let _ = tx.send(());
            }
        }
    }

    pub async fn kill_all(&self) {
        let metas: Vec<SessionMeta> = self.inner.lock().await.drain().map(|(_, m)| m).collect();
        for m in metas {
            m.expected_exit
                .store(true, std::sync::atomic::Ordering::SeqCst);
            if let Some(tx) = m.handle.cancel_tx {
                let _ = tx.send(());
            }
        }
    }
```

create_session 构造 SessionMeta 处补字段：

```rust
        self.inner.lock().await.insert(
            session_id.clone(),
            SessionMeta {
                session_id: session_id.clone(),
                handle,
                expected_exit,
            },
        );
```

- [ ] **Step 4: 确认通过 + 回归**

Run: `cargo test -p acp-claude --test crash_terminal_event -- --nocapture 2>&1 | tail -6`
Expected: 2 passed。

Run: `cargo test --workspace 2>&1 | grep -E "test result|error"`
Expected: 全部 ok。

**此 Task 不单独 commit。**

---

## Task 9: D6c — router 处理 terminal Error（❌ 卡 + 摘映射）

**Files:**
- Modify: `router/src/router.rs:127-147`（apply_event_to_out 的 match 臂拆分）
- Test: `router/tests/terminal_error_test.rs`（新建）

**Interfaces:**
- Consumes: Task 7 的 `Error.terminal`；`SessionMap::remove_by_session`（state.rs:57-66，已存在）
- Produces: terminal Error → `Out::UpdateCard`（❌ 标题 + ❌ 正文）+ 映射摘除；非 terminal Error → 维持现状

- [ ] **Step 1: 写失败测试**

新建 `router/tests/terminal_error_test.rs`：

```rust
//! Terminal AcpEvent::Error: the router must remove the session mapping and
//! emit an ❌ UpdateCard. Non-terminal errors keep the existing behaviour.

use acp_claude::session::AcpEvent;
use router::router::{Out, RouterHandle};
use router::state::{Mapping, SessionMap};
use std::time::Duration;
use feishu::events::SessionKey;

#[tokio::test]
async fn terminal_error_removes_mapping_and_marks_card() {
    let map = SessionMap::new();
    let key = SessionKey {
        chat_id: "oc_x".into(),
        thread_id: None,
    };
    map.insert(
        key.clone(),
        Mapping {
            session_id: "s1".into(),
            last_active_unix: 1,
        },
    )
    .await
    .unwrap();
    let (router, mut out_rx) = RouterHandle::new(map.clone());

    router
        .dispatch_acp_event(AcpEvent::Error {
            session_id: "s1".into(),
            message: "agent process exited".into(),
            terminal: true,
        })
        .await;

    let out = tokio::time::timeout(Duration::from_millis(200), out_rx.recv())
        .await
        .unwrap()
        .unwrap();
    match out {
        Out::UpdateCard { session_id, card } => {
            assert_eq!(session_id, "s1");
            let s = serde_json::to_string(&card).unwrap();
            assert!(s.contains('❌'), "expected ❌ in terminal card: {s}");
        }
        other => panic!("expected UpdateCard, got {other:?}"),
    }
    assert!(
        map.get(&key).await.is_none(),
        "terminal error must remove the session mapping"
    );
}

#[tokio::test]
async fn non_terminal_error_keeps_mapping() {
    let map = SessionMap::new();
    let key = SessionKey {
        chat_id: "oc_x".into(),
        thread_id: None,
    };
    map.insert(
        key.clone(),
        Mapping {
            session_id: "s1".into(),
            last_active_unix: 1,
        },
    )
    .await
    .unwrap();
    let (router, mut out_rx) = RouterHandle::new(map.clone());

    router
        .dispatch_acp_event(AcpEvent::Error {
            session_id: "s1".into(),
            message: "minor".into(),
            terminal: false,
        })
        .await;

    let out = tokio::time::timeout(Duration::from_millis(200), out_rx.recv())
        .await
        .unwrap()
        .unwrap();
    assert!(matches!(out, Out::UpdateCard { .. }));
    assert!(
        map.get(&key).await.is_some(),
        "non-terminal error must keep the mapping"
    );
}
```

- [ ] **Step 2: 确认失败**

Run: `cargo test -p router --test terminal_error_test 2>&1 | tail -6`
Expected: FAIL——`terminal_error_removes_mapping_and_marks_card` 断言映射仍在 / 卡片无 ❌（旧代码 Error 与 TextDelta 共用 🚧 臂且不摘映射）。

- [ ] **Step 3: 修改 apply_event_to_out**

`router/src/router.rs` 的 match（127-147 行）拆分为两个臂：

```rust
        match event {
            AcpEvent::Error {
                session_id,
                terminal: true,
                ..
            } => {
                // Session is unrecoverably dead: drop the mapping so later
                // messages don't route into a corpse, and mark the root card.
                self.map.remove_by_session(session_id).await;
                let mut card = render_root_card("", session_id, "❌");
                apply_event(&mut card, event);
                let _ = self
                    .tx
                    .send(Out::UpdateCard {
                        session_id: session_id.clone(),
                        card: serde_json::to_value(&card).unwrap(),
                    })
                    .await;
            }
            AcpEvent::TextDelta { .. }
            | AcpEvent::ToolStart { .. }
            | AcpEvent::Finished { .. }
            | AcpEvent::Error { .. } => {
                let emoji = if matches!(event, AcpEvent::Finished { .. }) {
                    "✅"
                } else {
                    "🚧"
                };
                let mut card = render_root_card("", &session_id, emoji);
                apply_event(&mut card, event);
                let _ = self
                    .tx
                    .send(Out::UpdateCard {
                        session_id,
                        card: serde_json::to_value(&card).unwrap(),
                    })
                    .await;
            }
```

（PermissionRequest 臂与 `_ => {}` 不变。注意 guarded 臂必须放在共享臂之前。）

- [ ] **Step 4: 确认通过 + 回归**

Run: `cargo test -p router 2>&1 | grep "test result"`
Expected: 全部 ok。

**此 Task 不单独 commit。**

---

## Task 10: D7 — TokenManager 按需刷新 + commit 2

**Files:**
- Modify: `feishu/src/client.rs`（新增 TokenManager；send_card/update_card/react 改造）
- Modify: `src/run.rs:46-57`（启动 token 段）、`src/run.rs:116-139`（出站泵捕获）、`src/run.rs:218-297`（dispatch_out 签名与调用）
- Test: `feishu/tests/token_manager_test.rs`（新建）

**Interfaces:**
- Consumes: `FeishuToken`（client.rs，需补 `#[derive(Clone)]`）
- Produces:
  - `TokenManager::new(app_id: String, app_secret: String) -> Self`（官方 endpoint）
  - `TokenManager::with_url(app_id, app_secret, token_url) -> Self`（测试指向本地 stub）
  - `TokenManager::with_stub_token(access_token: &str) -> Self`（SEBAS_TEST_FAKE_TOKEN 路径）
  - `async fn token(&self) -> anyhow::Result<String>`（过期自动重取）
  - `async fn force_refresh(&self) -> anyhow::Result<()>`
  - `FeishuClient::send_card(&self, http, tokens: &TokenManager, key, card) -> Result<String>`（`update_card`/`react` 同签名变化；业务 `code != 0` → force_refresh + 重试一次）

- [ ] **Step 1: 写失败测试**

新建 `feishu/tests/token_manager_test.rs`：

```rust
//! TokenManager: lazy fetch on first use, expiry-triggered refetch,
//! force_refresh, and the send_card retry-once-on-business-error policy.
//! Backed by a hand-rolled TCP stub (no external mock deps).

use feishu::client::TokenManager;
use feishu::events::SessionKey;
use std::io::{BufRead, BufReader, Read, Write};
use std::net::TcpListener;
use std::sync::atomic::{AtomicUsize, Ordering};
use std::sync::Arc;

struct Stub {
    base: String,
    token_hits: Arc<AtomicUsize>,
    card_hits: Arc<AtomicUsize>,
    handle: std::thread::JoinHandle<()>,
}

/// Serves `token_body` for POST /token and a script of card responses for
/// POST /card. One request per connection (`connection: close`) so reqwest
/// never pipelines.
fn start_stub(card_bodies: Vec<String>) -> Stub {
    let listener = TcpListener::bind("127.0.0.1:0").unwrap();
    let port = listener.local_addr().unwrap().port();
    let token_hits = Arc::new(AtomicUsize::new(0));
    let card_hits = Arc::new(AtomicUsize::new(0));
    let (th, ch) = (token_hits.clone(), card_hits.clone());
    let handle = std::thread::spawn(move || {
        let mut card_bodies = card_bodies.into_iter();
        for stream in listener.incoming() {
            let mut stream = match stream {
                Ok(s) => s,
                Err(_) => break,
            };
            // Read request head + body (content-length).
            let mut reader = BufReader::new(stream.try_clone().unwrap());
            let mut head = String::new();
            let mut content_len = 0usize;
            loop {
                let mut line = String::new();
                if reader.read_line(&mut line).unwrap_or(0) == 0 {
                    break;
                }
                let lower = line.to_lowercase();
                if lower.starts_with("content-length:") {
                    content_len = lower[15..].trim().parse().unwrap_or(0);
                }
                if line == "\r\n" {
                    break;
                }
                head.push_str(&line);
            }
            let mut body = vec![0u8; content_len];
            let _ = reader.read_exact(&mut body);
            let path = head.split_whitespace().nth(1).unwrap_or("").to_string();
            let body_out = if path.starts_with("/token") {
                th.fetch_add(1, Ordering::SeqCst);
                r#"{"code":0,"msg":"ok","tenant_access_token":"t-fresh","expire":7200}"#.to_string()
            } else {
                ch.fetch_add(1, Ordering::SeqCst);
                card_bodies
                    .next()
                    .unwrap_or_else(|| r#"{"code":0,"msg":"ok","data":{"message_id":"om_1"}}"#.to_string())
            };
            let resp = format!(
                "HTTP/1.1 200 OK\r\ncontent-type: application/json\r\ncontent-length: {}\r\nconnection: close\r\n\r\n{}",
                body_out.len(),
                body_out
            );
            let _ = stream.write_all(resp.as_bytes());
            let _ = stream.flush();
        }
    });
    Stub {
        base: format!("http://127.0.0.1:{port}"),
        token_hits,
        card_hits,
        handle,
    }
}

#[tokio::test]
async fn fetches_lazily_and_caches_until_expiry() {
    let stub = start_stub(vec![]);
    let tm = TokenManager::with_url(
        "app".into(),
        "secret".into(),
        format!("{}/token", stub.base),
    );
    let t1 = tm.token().await.expect("first fetch");
    assert_eq!(t1, "t-fresh");
    let t2 = tm.token().await.expect("cached");
    assert_eq!(t2, "t-fresh");
    assert_eq!(
        stub.token_hits.load(Ordering::SeqCst),
        1,
        "second token() must be served from cache"
    );
    tm.force_refresh().await.expect("refresh");
    assert_eq!(stub.token_hits.load(Ordering::SeqCst), 2);
    drop(stub.handle);
}

#[tokio::test]
async fn send_card_retries_once_with_fresh_token_on_business_error() {
    // First card attempt: business error (e.g. invalid token). Retry: success.
    let stub = start_stub(vec![
        r#"{"code":99991663,"msg":"tenant access token invalid"}"#.to_string(),
        r#"{"code":0,"msg":"ok","data":{"message_id":"om_ok"}}"#.to_string(),
    ]);
    let tm = TokenManager::with_url(
        "app".into(),
        "secret".into(),
        format!("{}/token", stub.base),
    );
    // Point the client at the stub by using send_card's http + stub URL:
    // send_card builds its own URL, so for the stub we exercise the retry
    // helper through a card endpoint the stub serves. FeishuClient::send_card
    // targets the real Feishu host; for the test we call the internal
    // `post_card` via a client whose base is the stub. To keep the public
    // surface minimal, the retry policy is factored into
    // `FeishuClient::post_json_with_retry`, which the test drives directly.
    let client = feishu::client::FeishuClient::new(feishu::client::FeishuConfig {
        app_id: "app".into(),
        app_secret: "secret".into(),
        owner_id: String::new(),
    });
    let http = reqwest::Client::new();
    let key = SessionKey {
        chat_id: "oc_x".into(),
        thread_id: None,
    };
    let out = client
        .post_card_with_retry(
            &http,
            &tm,
            &format!("{}/card", stub.base),
            serde_json::json!({"receive_id": key.chat_id}),
        )
        .await
        .expect("retry succeeds");
    assert_eq!(out, "om_ok");
    assert_eq!(stub.card_hits.load(Ordering::SeqCst), 2, "exactly one retry");
    assert!(
        stub.token_hits.load(Ordering::SeqCst) >= 2,
        "initial token + forced refresh"
    );
    drop(stub.handle);
}
```

**设计说明（实现接口随之确定）**：重试策略收敛为一个 crate 内公开助手 `FeishuClient::post_card_with_retry(http, tokens, url, body) -> Result<String>`（返回 message_id），`send_card` 变为构造官方 URL 后调它；`update_card`/`react` 走同模式的 `post_with_retry`（返回 `()`）。这样出站三方法共享同一份"业务 code != 0 → 强制刷新重试一次"逻辑，且测试无需打真实飞书域名。

- [ ] **Step 2: 确认失败**

Run: `cargo test -p feishu --test token_manager_test 2>&1 | tail -4`
Expected: 编译 FAIL——`TokenManager` / `post_card_with_retry` 不存在。

- [ ] **Step 3: 实现 TokenManager + 出站改造**

`feishu/src/client.rs` 顶部补 import：

```rust
use tokio::sync::Mutex;
```

`FeishuToken` 补 Clone：

```rust
#[derive(Debug, Clone)]
pub struct FeishuToken {
    pub access_token: String,
    pub expires_at: i64,
}
```

新增（放在 `FeishuClient` 之后）：

```rust
/// Holds the tenant_access_token and refreshes it on demand.
/// `state` starts empty/expired so the first `token()` call fetches.
#[derive(Clone)]
pub struct TokenManager {
    http: reqwest::Client,
    app_id: String,
    app_secret: String,
    token_url: String,
    state: std::sync::Arc<Mutex<FeishuToken>>,
}

impl TokenManager {
    pub fn new(app_id: String, app_secret: String) -> Self {
        Self::with_url(
            app_id,
            app_secret,
            "https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal".into(),
        )
    }

    pub fn with_url(app_id: String, app_secret: String, token_url: String) -> Self {
        Self {
            http: reqwest::Client::new(),
            app_id,
            app_secret,
            token_url,
            state: std::sync::Arc::new(Mutex::new(FeishuToken {
                access_token: String::new(),
                expires_at: 0,
            })),
        }
    }

    /// Test hook (SEBAS_TEST_FAKE_TOKEN): preset a valid token; no HTTP is
    /// needed until it expires an hour later.
    pub fn with_stub_token(access_token: &str) -> Self {
        Self {
            http: reqwest::Client::new(),
            app_id: String::new(),
            app_secret: String::new(),
            token_url: String::new(),
            state: std::sync::Arc::new(Mutex::new(FeishuToken {
                access_token: access_token.into(),
                expires_at: chrono::Utc::now().timestamp() + 3600,
            })),
        }
    }

    pub async fn token(&self) -> anyhow::Result<String> {
        let mut g = self.state.lock().await;
        if chrono::Utc::now().timestamp() >= g.expires_at {
            *g = self.fetch().await?;
        }
        Ok(g.access_token.clone())
    }

    pub async fn force_refresh(&self) -> anyhow::Result<()> {
        let fresh = self.fetch().await?;
        *self.state.lock().await = fresh;
        Ok(())
    }

    async fn fetch(&self) -> anyhow::Result<FeishuToken> {
        let body = serde_json::json!({
            "app_id": self.app_id,
            "app_secret": self.app_secret,
        });
        let resp: TokenResponse = self
            .http
            .post(&self.token_url)
            .json(&body)
            .send()
            .await?
            .json()
            .await?;
        if resp.code != 0 {
            anyhow::bail!("feishu auth failed: code={} msg={}", resp.code, resp.msg);
        }
        Ok(FeishuToken {
            access_token: resp.tenant_access_token,
            expires_at: chrono::Utc::now().timestamp() + resp.expire as i64 - 60,
        })
    }
}
```

`FeishuClient` 的三个出站方法改造（保留 `fetch_token` 不动以兼容——它不再被 run.rs 使用，删除它及其独占 import 若 clippy 报 dead_code；保留亦可，优先删除）：

```rust
impl FeishuClient {
    /// POST a card-related JSON body with the shared retry policy:
    /// any business `code != 0` forces a token refresh and exactly one retry.
    pub async fn post_card_with_retry(
        &self,
        http: &reqwest::Client,
        tokens: &TokenManager,
        url: &str,
        body: serde_json::Value,
    ) -> anyhow::Result<String> {
        #[derive(serde::Deserialize)]
        struct R {
            code: i32,
            msg: String,
            #[serde(default)]
            data: MessageOut,
        }
        let mut attempt = 0;
        loop {
            let token = tokens.token().await?;
            let resp: R = http
                .post(url)
                .bearer_auth(token)
                .json(&body)
                .send()
                .await?
                .json()
                .await?;
            if resp.code == 0 {
                return Ok(resp.data.message_id.unwrap_or_default());
            }
            attempt += 1;
            if attempt > 1 {
                anyhow::bail!("feishu api failed after token refresh: {} {}", resp.code, resp.msg);
            }
            tokens.force_refresh().await?;
        }
    }

    /// Same retry policy for endpoints that return no payload.
    pub async fn post_with_retry(
        &self,
        http: &reqwest::Client,
        tokens: &TokenManager,
        url: &str,
        body: serde_json::Value,
    ) -> anyhow::Result<()> {
        #[derive(serde::Deserialize)]
        struct R {
            code: i32,
            msg: String,
        }
        let mut attempt = 0;
        loop {
            let token = tokens.token().await?;
            let resp: R = http
                .post(url)
                .bearer_auth(token)
                .json(&body)
                .send()
                .await?
                .json()
                .await?;
            if resp.code == 0 {
                return Ok(());
            }
            attempt += 1;
            if attempt > 1 {
                anyhow::bail!("feishu api failed after token refresh: {} {}", resp.code, resp.msg);
            }
            tokens.force_refresh().await?;
        }
    }

    pub async fn send_card(
        &self,
        http: &reqwest::Client,
        tokens: &TokenManager,
        key: &SessionKey,
        card_json: serde_json::Value,
    ) -> anyhow::Result<String> {
        let url = "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id";
        let body = serde_json::json!({
            "receive_id": key.chat_id,
            "msg_type": "interactive",
            "content": serde_json::to_string(&card_json)?,
        });
        self.post_card_with_retry(http, tokens, url, body).await
    }

    pub async fn update_card(
        &self,
        http: &reqwest::Client,
        tokens: &TokenManager,
        message_id: &str,
        card_json: serde_json::Value,
    ) -> anyhow::Result<()> {
        let url = format!("https://open.feishu.cn/open-apis/im/v1/messages/{message_id}");
        let body = serde_json::json!({ "content": serde_json::to_string(&card_json)? });
        self.post_with_retry(http, tokens, url, body).await
    }

    pub async fn react(
        &self,
        http: &reqwest::Client,
        tokens: &TokenManager,
        message_id: &str,
        emoji_type: &str,
    ) -> anyhow::Result<()> {
        let url = format!(
            "https://open.feishu.cn/open-apis/im/v1/messages/{message_id}/reactions"
        );
        let body = serde_json::json!({ "reaction_type": { "emoji_type": emoji_type } });
        self.post_with_retry(http, tokens, url, body).await
    }
}
```

注意：`update_card`/`react` 原来是 PATCH/POST——飞书 update 用 PATCH。`post_with_retry` 统一 POST 会破坏 update_card（HTTP 方法语义）。处理：`post_with_retry` 增加 `method: reqwest::Method` 参数，调用方传 `Method::PATCH` 或 `Method::POST`。相应测试只覆盖 `post_card_with_retry`（POST），PATCH 路径由相同代码结构保证。签名定为：

```rust
    pub async fn request_with_retry(
        &self,
        http: &reqwest::Client,
        tokens: &TokenManager,
        method: reqwest::Method,
        url: &str,
        body: serde_json::Value,
    ) -> anyhow::Result<()> { /* 同上结构，http.request(method, url)... */ }
```

`update_card` 调 `request_with_retry(..., reqwest::Method::PATCH, ...)`，`react` 调 `Method::POST`。`post_with_retry` 不需要单独存在。

`run.rs` 启动段（46-57 行）替换：

```rust
    let tokens = if std::env::var("SEBAS_TEST_FAKE_TOKEN").as_deref() == Ok("1") {
        info!("SEBAS_TEST_FAKE_TOKEN=1; using stub tenant_access_token");
        feishu::client::TokenManager::with_stub_token("t-stub-test")
    } else {
        let tm = feishu::client::TokenManager::new(
            cfg.feishu.app_id.clone(),
            cfg.feishu.app_secret.clone(),
        );
        // Startup auth check stays fatal (spec §4.1).
        tm.token()
            .await
            .map_err(|e| crate::error::SebasError::Feishu(e.to_string()))?;
        tm
    };
```

hello_msg 段（68-71 行）`&token.access_token` → `tokens.token().await.unwrap_or_default()` 的局部变量 `bearer`；test_msg 段（94-98 行）同理。

出站泵（116-139 行）：`let token_clone = token.access_token.clone();` 删除，改为 `let tokens_for_outbound = tokens.clone();`，dispatch_out 实参 `&tokens_for_outbound`。

dispatch_out 签名：

```rust
async fn dispatch_out(
    feishu: &FeishuClient,
    http: &reqwest::Client,
    tokens: &feishu::client::TokenManager,
    cfg: &Config,
    router: &RouterHandle,
    mgr: &Arc<SessionManager>,
    out: Out,
) -> anyhow::Result<()> {
```

体内 `feishu.send_card(http, token, ...)` → `feishu.send_card(http, tokens, ...)`；`update_card`/`react` 同理。

- [ ] **Step 4: 确认通过 + 回归**

Run: `cargo test -p feishu --test token_manager_test -- --nocapture 2>&1 | tail -5`
Expected: 2 passed。

Run: `cargo test --workspace 2>&1 | grep -E "test result|error"`
Expected: 全部 ok。

Run: `cargo test --workspace -- --ignored --nocapture 2>&1 | grep -E "test result"`
Expected: sigterm 测试也通过（SEBAS_TEST_FAKE_TOKEN 路径走的是 `with_stub_token`）。

- [ ] **Step 5: Commit 2（组 2）**

```bash
git add acp-claude/src/manager.rs acp-claude/src/session.rs \
  acp-claude/tests/startup_timeout.rs acp-claude/tests/crash_terminal_event.rs \
  acp-claude/tests/parse_test.rs acp-claude/tests/canned_test.rs \
  acp-claude/tests/lifecycle_test.rs acp-claude/tests/no_duplicate_prompt.rs \
  acp-claude/tests/single_session_new.rs acp-claude/tests/cancel_keeps_session.rs \
  acp-claude/tests/permission_roundtrip.rs \
  router/src/router.rs router/tests/terminal_error_test.rs \
  feishu/src/client.rs feishu/tests/token_manager_test.rs src/run.rs
git commit -m "fix(acp-claude,feishu): session lifecycle (startup timeout, terminal crash event, token refresh)"
```

```bash
bd update sebas-rfs --status=in_progress 2>/dev/null; bd close sebas-rfs --reason="组2完成：startup_timeout 接线+超时回收、崩溃终态事件合成+表清理+❌卡、TokenManager 按需刷新+重试一次，测试全绿"
```

---

## Task 11: D8 — MappingState 占位态 + 双重 spawn 竞态

**Files:**
- Modify: `router/src/state.rs`（MappingState、route_text/activate/fail_spawn/begin_spawn、dump 过滤）
- Modify: `router/src/router.rs`（on_text PassThrough/New 臂、insert_mapping、session_alive）
- Modify: `feishu/src/cards.rs`（新增 `render_error_card`）
- Modify: `src/run.rs:253-288`（SpawnAcp 臂：失败发 ❌ 卡 + activate + pending 补发）
- Test: `router/tests/spawn_race_test.rs`（新建）
- Test: `router/tests/state_test.rs`、`router/tests/router_test.rs`、`router/tests/permission_test.rs`、`router/tests/terminal_error_test.rs`（Mapping 字面量迁移 + 顺手修 4 处 unused-Result 警告）

**Interfaces:**
- Consumes: `RouterError::Capacity`（state.rs 已有）
- Produces:
  - `MappingState { Spawning { pending: Vec<String> }, Active { session_id: String } }`
  - `Mapping { state: MappingState, last_active_unix: i64 }`；`Mapping::active(impl Into<String>)`、`Mapping::spawning()`、`Mapping::session_id() -> Option<&str>`
  - `TextRoute { SpawnNew, Continue(String), Enqueued }`
  - `SessionMap::route_text(&self, key: SessionKey, prompt: String) -> Result<TextRoute, RouterError>`（单次写锁内 check+act，消除跨 await 窗口）
  - `SessionMap::begin_spawn(&self, key: SessionKey) -> Result<(), RouterError>`（/new 用：无条件占位覆盖）
  - `SessionMap::activate(&self, key: &SessionKey, session_id: String) -> Vec<String>`（翻转 + drain pending）
  - `SessionMap::fail_spawn(&self, key: &SessionKey)`（仅摘除 Spawning 占位）
  - `RouterHandle::activate(&self, key: &SessionKey, session_id: String) -> Vec<String>` / `RouterHandle::fail_spawn(&self, key: &SessionKey)`（SessionMap 透传，供 run.rs 使用）
  - `render_error_card(message: &str) -> Card`
  - pending 上限 **16**，溢出 `warn!` 并丢弃最新

- [ ] **Step 1: 写失败测试**

新建 `router/tests/spawn_race_test.rs`：

```rust
//! D8: two texts racing a slow spawn must yield exactly one SpawnAcp; the
//! second is queued and drained by activate(). Disk format stays legacy.

use feishu::events::{FeishuIn, SessionKey};
use router::router::{Out, RouterHandle};
use router::state::{Mapping, MappingState, SessionMap, TextRoute};
use std::time::Duration;

fn key() -> SessionKey {
    SessionKey {
        chat_id: "oc_race".into(),
        thread_id: None,
    }
}

#[tokio::test]
async fn second_text_during_spawn_is_queued_not_spawned() {
    let map = SessionMap::new();
    let (router, mut out_rx) = RouterHandle::new(map.clone());

    router
        .dispatch(FeishuIn::Text {
            key: key(),
            text: "msg1".into(),
            reply_to: None,
        })
        .await;
    let first = tokio::time::timeout(Duration::from_millis(200), out_rx.recv())
        .await
        .unwrap()
        .unwrap();
    assert!(matches!(first, Out::SpawnAcp { .. }), "got {first:?}");

    // Spawn still in flight (nobody called activate): the second text queues.
    router
        .dispatch(FeishuIn::Text {
            key: key(),
            text: "msg2".into(),
            reply_to: None,
        })
        .await;
    let second = tokio::time::timeout(Duration::from_millis(150), out_rx.recv()).await;
    assert!(second.is_err(), "no second Out may be emitted while spawning");

    let pending = map.activate(&key(), "s1".into()).await;
    assert_eq!(pending, vec!["msg2".to_string()]);
    // Now active: a third text continues the session.
    router
        .dispatch(FeishuIn::Text {
            key: key(),
            text: "msg3".into(),
            reply_to: None,
        })
        .await;
    let third = tokio::time::timeout(Duration::from_millis(200), out_rx.recv())
        .await
        .unwrap()
        .unwrap();
    match third {
        Out::SendAcp { session_id, .. } => assert_eq!(session_id, "s1"),
        other => panic!("expected SendAcp, got {other:?}"),
    }
}

#[tokio::test]
async fn fail_spawn_removes_placeholder() {
    let map = SessionMap::new();
    map.route_text(key(), "m".into())
        .await
        .expect("route_text");
    map.fail_spawn(&key()).await;
    assert!(map.get(&key()).await.is_none());
}

#[tokio::test]
async fn pending_queue_capped_at_16() {
    let map = SessionMap::new();
    map.route_text(key(), "m0".into()).await.unwrap();
    for i in 1..20 {
        let r = map.route_text(key(), format!("m{i}")).await.unwrap();
        assert!(matches!(r, TextRoute::Enqueued));
    }
    let pending = map.activate(&key(), "s1".into()).await;
    assert_eq!(pending.len(), 16);
    assert_eq!(pending[0], "m1");
    assert_eq!(pending[15], "m16");
}

#[tokio::test]
async fn dump_filters_spawning_and_keeps_legacy_shape() {
    let map = SessionMap::new();
    map.route_text(key(), "m".into()).await.unwrap();
    let active_key = SessionKey {
        chat_id: "oc_active".into(),
        thread_id: None,
    };
    map.insert(active_key.clone(), Mapping::active("s9")).await.unwrap();

    let json = map.dump_json().await.unwrap();
    assert!(!json.contains("oc_race"), "spawning entry leaked into dump");
    assert!(json.contains("oc_active"));
    // Legacy disk shape: plain {"session_id": ..., "last_active_unix": ...}.
    assert!(json.contains("\"session_id\":\"s9\""));
    assert!(!json.contains("Spawning") && !json.contains("spawning"));

    // Round-trip through restore.
    let restored = SessionMap::restore_json(&json).unwrap();
    let m = restored.get(&active_key).await.expect("restored");
    assert!(matches!(m.state, MappingState::Active { .. }));
    assert_eq!(m.session_id(), Some("s9"));
}
```

- [ ] **Step 2: 确认失败**

Run: `cargo test -p router --test spawn_race_test 2>&1 | tail -4`
Expected: 编译 FAIL（`MappingState`/`TextRoute`/`route_text`/`activate`/`fail_spawn` 不存在）。

- [ ] **Step 3: 重写 state.rs**

完整替换 `router/src/state.rs`：

```rust
use crate::error::RouterError;
use feishu::events::SessionKey;
use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Arc;
use tokio::sync::RwLock;

/// In-memory session mapping state. `Spawning` is a placeholder inserted
/// synchronously when the first text arrives, so a second text racing the
/// (slow) ACP spawn is queued instead of spawning a duplicate child.
/// `Spawning` is never persisted (the child is gone after a restart anyway).
#[derive(Debug, Clone)]
pub enum MappingState {
    Spawning { pending: Vec<String> },
    Active { session_id: String },
}

#[derive(Debug, Clone)]
pub struct Mapping {
    pub state: MappingState,
    pub last_active_unix: i64,
}

impl Mapping {
    pub fn active(session_id: impl Into<String>) -> Self {
        Self {
            state: MappingState::Active {
                session_id: session_id.into(),
            },
            last_active_unix: crate::router::now_unix(),
        }
    }

    pub fn spawning() -> Self {
        Self {
            state: MappingState::Spawning { pending: Vec::new() },
            last_active_unix: crate::router::now_unix(),
        }
    }

    pub fn session_id(&self) -> Option<&str> {
        match &self.state {
            MappingState::Active { session_id } => Some(session_id),
            MappingState::Spawning { .. } => None,
        }
    }
}

/// What the router should do with an inbound text, decided atomically under
/// a single write lock (no check-then-act window).
pub enum TextRoute {
    /// No mapping existed; a Spawning placeholder was inserted. Spawn now.
    SpawnNew,
    /// A live session exists; forward the prompt to this session_id.
    Continue(String),
    /// A spawn is already in flight; the prompt was queued (or dropped with a
    /// warning when the queue is full).
    Enqueued,
}

const MAX_PENDING: usize = 16;

#[derive(Clone)]
pub struct SessionMap {
    inner: Arc<RwLock<HashMap<SessionKey, Mapping>>>,
    capacity: usize,
}

impl SessionMap {
    pub fn new() -> Self {
        Self::with_capacity(usize::MAX)
    }
    pub fn with_capacity(cap: usize) -> Self {
        Self {
            inner: Arc::new(RwLock::new(HashMap::new())),
            capacity: cap,
        }
    }

    /// Atomic check+act for an inbound text (D8 fix).
    pub async fn route_text(&self, key: SessionKey, prompt: String) -> Result<TextRoute, RouterError> {
        let mut g = self.inner.write().await;
        match g.get_mut(&key) {
            None => {
                if g.len() >= self.capacity {
                    return Err(RouterError::Capacity(self.capacity));
                }
                g.insert(key, Mapping::spawning());
                Ok(TextRoute::SpawnNew)
            }
            Some(m) => match &mut m.state {
                MappingState::Active { session_id } => {
                    Ok(TextRoute::Continue(session_id.clone()))
                }
                MappingState::Spawning { pending } => {
                    if pending.len() < MAX_PENDING {
                        pending.push(prompt);
                    } else {
                        tracing::warn!("pending queue full; dropping newest message");
                    }
                    Ok(TextRoute::Enqueued)
                }
            },
        }
    }

    /// `/new`: unconditionally (re)place a Spawning placeholder, so texts
    /// arriving during the respawn queue instead of double-spawning.
    pub async fn begin_spawn(&self, key: SessionKey) -> Result<(), RouterError> {
        let mut g = self.inner.write().await;
        if !g.contains_key(&key) && g.len() >= self.capacity {
            return Err(RouterError::Capacity(self.capacity));
        }
        g.insert(key, Mapping::spawning());
        Ok(())
    }

    /// Flip Spawning -> Active and drain queued prompts (returned in order).
    pub async fn activate(&self, key: &SessionKey, session_id: String) -> Vec<String> {
        let mut g = self.inner.write().await;
        match g.get_mut(key) {
            Some(m) => {
                match std::mem::replace(
                    &mut m.state,
                    MappingState::Active { session_id },
                ) {
                    MappingState::Spawning { pending } => pending,
                    MappingState::Active { .. } => Vec::new(),
                }
            }
            None => {
                tracing::warn!(?key, "activate without placeholder; inserting fresh mapping");
                g.insert(key.clone(), Mapping::active(session_id));
                Vec::new()
            }
        }
    }

    /// Spawn failed: remove the placeholder (queued prompts drop with it).
    /// Only touches Spawning entries — never an Active session.
    pub async fn fail_spawn(&self, key: &SessionKey) {
        let mut g = self.inner.write().await;
        let is_spawning = matches!(
            g.get(key).map(|m| &m.state),
            Some(MappingState::Spawning { .. })
        );
        if is_spawning {
            g.remove(key);
        }
    }

    pub async fn insert(&self, key: SessionKey, mapping: Mapping) -> Result<(), RouterError> {
        let mut g = self.inner.write().await;
        if !g.contains_key(&key) && g.len() >= self.capacity {
            return Err(RouterError::Capacity(self.capacity));
        }
        g.insert(key, mapping);
        Ok(())
    }

    pub async fn get(&self, key: &SessionKey) -> Option<Mapping> {
        self.inner.read().await.get(key).cloned()
    }

    pub async fn lookup_key_by_session(&self, session_id: &str) -> Option<SessionKey> {
        self.inner
            .read()
            .await
            .iter()
            .find(|(_, m)| m.session_id() == Some(session_id))
            .map(|(k, _)| k.clone())
    }

    pub async fn remove_by_session(&self, session_id: &str) {
        let mut g = self.inner.write().await;
        if let Some(k) = g
            .iter()
            .find(|(_, m)| m.session_id() == Some(session_id))
            .map(|(k, _)| k.clone())
        {
            g.remove(&k);
        }
    }

    /// Persist ONLY Active entries, in the legacy flat shape
    /// (`{"session_id": ..., "last_active_unix": ...}`) so the on-disk format
    /// is unchanged and restores work across versions.
    pub async fn dump_json(&self) -> serde_json::Result<String> {
        let g = self.inner.read().await;
        let active: HashMap<&SessionKey, MappingDto> = g
            .iter()
            .filter_map(|(k, m)| {
                m.session_id().map(|sid| {
                    (
                        k,
                        MappingDto {
                            session_id: sid.to_string(),
                            last_active_unix: m.last_active_unix,
                        },
                    )
                })
            })
            .collect();
        serde_json::to_string(&active)
    }

    pub fn restore_json(s: &str) -> serde_json::Result<Self> {
        let dto: HashMap<SessionKey, MappingDto> = serde_json::from_str(s)?;
        let map = dto
            .into_iter()
            .map(|(k, d)| {
                (
                    k,
                    Mapping {
                        state: MappingState::Active {
                            session_id: d.session_id,
                        },
                        last_active_unix: d.last_active_unix,
                    },
                )
            })
            .collect();
        Ok(Self {
            inner: Arc::new(RwLock::new(map)),
            capacity: usize::MAX,
        })
    }
}

impl Default for SessionMap {
    fn default() -> Self {
        Self::new()
    }
}

/// Legacy on-disk shape (unchanged).
#[derive(Serialize, Deserialize)]
struct MappingDto {
    session_id: String,
    last_active_unix: i64,
}
```

- [ ] **Step 4: 改 router.rs**

`insert_mapping`（现 89-97 行）：

```rust
    pub async fn insert_mapping(&self, key: SessionKey, session_id: String) {
        if let Err(e) = self.map.insert(key, crate::state::Mapping::active(session_id)).await {
            tracing::warn!(?e, "failed to insert session mapping");
        }
    }
```

`session_alive`（现 101-103 行）——仅 Active 视为存活：

```rust
    pub async fn session_alive(&self, key: &SessionKey) -> bool {
        self.map
            .get(key)
            .await
            .map(|m| m.session_id().is_some())
            .unwrap_or(false)
    }
```

新增两个 `SessionMap` 透传方法（run.rs 的 SpawnAcp 臂与 Task 12 的钩子函数经 `RouterHandle` 调用它们，无法直接触达私有字段 `map`）：

```rust
    /// Flip Spawning -> Active for `key` and drain queued prompts.
    /// Called by the dispatcher once `create_session` has minted the id.
    pub async fn activate(&self, key: &SessionKey, session_id: String) -> Vec<String> {
        self.map.activate(key, session_id).await
    }

    /// Spawn failed/timeout: remove the Spawning placeholder for `key`.
    pub async fn fail_spawn(&self, key: &SessionKey) {
        self.map.fail_spawn(key).await;
    }
```

`on_text` 的 `Command::New` 与 `Command::PassThrough` 臂（现 177、181-187 行）：

```rust
            Command::New => {
                if let Err(e) = self.map.begin_spawn(key.clone()).await {
                    tracing::warn!(?e, "begin_spawn failed");
                    let _ = self.tx.send(Out::HelpText { key }).await;
                } else {
                    self.spawn_new(key, String::new()).await;
                }
            }
```

```rust
            Command::PassThrough(p) => {
                match self.map.route_text(key.clone(), p.clone()).await {
                    Ok(crate::state::TextRoute::Continue(sid)) => {
                        self.continue_session(sid, p).await
                    }
                    Ok(crate::state::TextRoute::SpawnNew) => self.spawn_new(key, p).await,
                    Ok(crate::state::TextRoute::Enqueued) => {}
                    Err(e) => {
                        tracing::warn!(?e, "route_text failed");
                        let _ = self.tx.send(Out::HelpText { key }).await;
                    }
                }
            }
```

`Command::Compact | Cost | Cancel | Status` 臂里的 `m.session_id` 引用（现 189-190 行）改为只在 Active 时转发：

```rust
            Command::Compact | Command::Cost | Command::Cancel | Command::Status => {
                let sid = self.map.get(&key).await.and_then(|m| m.session_id().map(str::to_owned));
                if let Some(sid) = sid {
                    self.forward_to_session(&sid, text).await;
                } else {
                    let _ = self.tx.send(Out::HelpText { key }).await;
                }
            }
```

- [ ] **Step 5: cards.rs 加 render_error_card**

`feishu/src/cards.rs` 末尾（`render_dead_session_card` 之后）：

```rust
/// Shown when the agent fails to spawn or times out during handshake.
pub fn render_error_card(message: &str) -> Card {
    let mut card = Card::new("❌ 启动失败", "red");
    card.push_text(message.to_string());
    card
}
```

- [ ] **Step 6: run.rs 的 SpawnAcp 臂接线**

`src/run.rs:253-288` 整臂替换：

```rust
        Out::SpawnAcp { key, prompt } => {
            let claude = &cfg.acp.claude;
            // 1) Spawn the claude subprocess and mint a session_id. On failure
            //    (missing binary, handshake timeout): drop the router's
            //    Spawning placeholder and show an ❌ card instead of a silent
            //    log line (spec §4.1 "ACP spawn failure").
            let session_id = match mgr
                .create_session(
                    &claude.path,
                    claude.args.clone(),
                    claude.work_dir.clone(),
                    prompt.clone(),
                )
                .await
            {
                Ok(sid) => sid,
                Err(e) => {
                    router.fail_spawn(&key).await;
                    let card = feishu::cards::render_error_card(&format!(
                        "agent 启动失败/超时：{e}。请检查 claude 是否安装、PATH 是否正确。"
                    ));
                    if let Err(e2) = feishu
                        .send_card(http, tokens, &key, serde_json::to_value(&card)?)
                        .await
                    {
                        warn!(?e2, "failed to send spawn-failure card");
                    }
                    warn!(?e, "create_session failed");
                    return Ok(());
                }
            };
            // 2) Kick off the session with the initial prompt.
            mgr.send(
                &session_id,
                AcpCommand::CreateSession {
                    session_id: session_id.clone(),
                    prompt: prompt.clone(),
                },
            )
            .await?;
            // 3) Flip the router's Spawning placeholder to Active and drain
            //    any prompts that arrived while we were spawning.
            let pending = router.activate(&key, session_id.clone()).await;
            // 4) Send the root card and record its message_id keyed by the real
            //    session_id (so streaming UpdateCards resolve correctly). Done
            //    before the event pump starts so no early delta is lost.
            let card = render_root_card(&prompt, &session_id, "👀");
            let msg_id = feishu
                .send_card(http, tokens, &key, serde_json::to_value(&card)?)
                .await?;
            if !msg_id.is_empty() {
                router.record_root_msg_id(session_id.clone(), msg_id).await;
            }
            // 5) Pump ACP events from this session back into the router.
            spawn_acp_pump(mgr.clone(), router.clone(), session_id.clone());
            // 6) Flush queued prompts as ONE follow-up (sending them one by
            //    one would violate ACP's one-prompt-in-flight rule).
            if !pending.is_empty() {
                let joined = pending.join("\n");
                if let Err(e) = mgr
                    .send(
                        &session_id,
                        AcpCommand::ContinueSession {
                            session_id: session_id.clone(),
                            prompt: joined,
                        },
                    )
                    .await
                {
                    warn!(?e, "failed to flush pending prompts");
                }
            }
        }
```

注意：臂内 `feishu.send_card(http, token, ...)` 原有的 `token` 实参随 Task 10 已改为 `tokens`，保持一致。

- [ ] **Step 7: 迁移旧测试 + 修 4 处 unused-Result 警告**

`router/tests/state_test.rs`、`router/tests/router_test.rs`、`router/tests/permission_test.rs`、`router/tests/terminal_error_test.rs` 中所有：

```rust
Mapping {
    session_id: "s1".into(),
    last_active_unix: 1,
},
```

改为：

```rust
Mapping::active("s1"),
```

（`last_active_unix: 1` 的精确值无断言依赖；若有测试断言该字段，保留字面量写法并改用 `Mapping { state: MappingState::Active { session_id: "s1".into() }, last_active_unix: 1 }`。实现时以编译为准逐一处理。）

`state_test.rs:12,31,50` 与 `router_test.rs:41` 的 `m.insert(...).await;` 补 `.unwrap();`（消除 4 处 unused-Result 警告）。

- [ ] **Step 8: 确认通过 + 回归**

Run: `cargo test -p router 2>&1 | grep "test result"`
Expected: 全部 ok。

Run: `cargo test --workspace 2>&1 | grep -E "test result|error|warning: unused"`
Expected: 全部 ok，无 unused-Result 警告。

**此 Task 不单独 commit。**

---

## Task 12: 组 3 集成 — DELAY_NEW 竞态确定性复现 + commit 3

**Files:**
- Modify: `src/run.rs`（SpawnAcp 臂提取两个 pub 测试钩子函数）
- Test: `tests/spawn_race_test.rs`（新建，sebas crate 集成测试）

**Interfaces:**
- Consumes: Task 1 的 `--delay-new-ms`、Task 11 的 `activate`/pending
- Produces（pub，标注 doc-hidden 内部钩子的注释）:
  - `run::acp_spawn_and_activate(mgr: &Arc<SessionManager>, router: &RouterHandle, key: &SessionKey, prompt: &str, claude_path: &str, claude_args: Vec<String>, work_dir: Option<String>) -> anyhow::Result<(String, Vec<String>)>` — create_session + 首发 prompt + activate drain pending；不起 pump、不 flush
  - `run::flush_pending_prompts(mgr: &Arc<SessionManager>, session_id: &str, pending: Vec<String>) -> anyhow::Result<()>`

- [ ] **Step 1: 提取钩子函数**

`src/run.rs` 中，把 Task 11 Step 6 的 SpawnAcp 臂主体拆为（臂本身变薄，逻辑不变）：

```rust
/// Create the ACP session, send the initial prompt, and flip the router's
/// Spawning placeholder to Active (draining queued prompts). No Feishu side
/// effects, no event pump, no pending flush — the caller sequences those so
/// the root card can go out before the pump starts. `pub` for integration
/// tests (tests/spawn_race_test.rs); not part of the stable API.
pub async fn acp_spawn_and_activate(
    mgr: &Arc<SessionManager>,
    router: &RouterHandle,
    key: &SessionKey,
    prompt: &str,
    claude_path: &str,
    claude_args: Vec<String>,
    work_dir: Option<String>,
) -> anyhow::Result<(String, Vec<String>)> {
    let session_id = mgr
        .create_session(claude_path, claude_args, work_dir, prompt.to_string())
        .await?;
    mgr.send(
        &session_id,
        AcpCommand::CreateSession {
            session_id: session_id.clone(),
            prompt: prompt.to_string(),
        },
    )
    .await?;
    let pending = router.activate(key, session_id.clone()).await;
    Ok((session_id, pending))
}

/// Flush prompts queued during spawn as ONE ContinueSession (one-by-one
/// would violate ACP's one-prompt-in-flight rule). `pub` for tests.
pub async fn flush_pending_prompts(
    mgr: &Arc<SessionManager>,
    session_id: &str,
    pending: Vec<String>,
) -> anyhow::Result<()> {
    if pending.is_empty() {
        return Ok(());
    }
    mgr.send(
        session_id,
        AcpCommand::ContinueSession {
            session_id: session_id.to_string(),
            prompt: pending.join("\n"),
        },
    )
    .await
}
```

SpawnAcp 臂改为调用：

```rust
        Out::SpawnAcp { key, prompt } => {
            let claude = &cfg.acp.claude;
            let (session_id, pending) = match acp_spawn_and_activate(
                mgr, router, &key, &prompt,
                &claude.path, claude.args.clone(), claude.work_dir.clone(),
            )
            .await
            {
                Ok(v) => v,
                Err(e) => {
                    router.fail_spawn(&key).await;
                    let card = feishu::cards::render_error_card(&format!(
                        "agent 启动失败/超时：{e}。请检查 claude 是否安装、PATH 是否正确。"
                    ));
                    if let Err(e2) = feishu
                        .send_card(http, tokens, &key, serde_json::to_value(&card)?)
                        .await
                    {
                        warn!(?e2, "failed to send spawn-failure card");
                    }
                    warn!(?e, "create_session failed");
                    return Ok(());
                }
            };
            let card = render_root_card(&prompt, &session_id, "👀");
            let msg_id = feishu
                .send_card(http, tokens, &key, serde_json::to_value(&card)?)
                .await?;
            if !msg_id.is_empty() {
                router.record_root_msg_id(session_id.clone(), msg_id).await;
            }
            spawn_acp_pump(mgr.clone(), router.clone(), session_id.clone());
            if let Err(e) = flush_pending_prompts(mgr, &session_id, pending).await {
                warn!(?e, "failed to flush pending prompts");
            }
        }
```

注意：`create_session` 失败与 `CreateSession` prompt 发送失败都走同一 Err 分支（都会 fail_spawn + ❌ 卡）——后者其实也代表 session 不可用，语义正确。`?` 传播被显式处理取代。

- [ ] **Step 2: 写集成测试**

新建 `tests/spawn_race_test.rs`：

```rust
//! Group-3 integration: with a slow session/new (fake --delay-new-ms), two
//! rapid texts produce exactly ONE session/new and TWO session/prompt calls
//! (the second being the drained pending queue, joined).

use feishu::events::{FeishuIn, SessionKey};
use router::router::{Out, RouterHandle};
use router::state::SessionMap;
use acp_claude::manager::SessionManager;
use std::path::PathBuf;
use std::sync::Arc;
use std::time::Duration;

fn fake() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("target/debug/fake-claude")
}

#[tokio::test]
async fn racing_texts_yield_one_spawn_and_joined_pending() {
    let journal = std::env::temp_dir().join(format!(
        "fc-journal-{}-race-{}.jsonl",
        std::process::id(),
        std::time::SystemTime::now()
            .duration_since(std::time::UNIX_EPOCH)
            .map(|d| d.as_nanos())
            .unwrap_or(0)
    ));
    let map = SessionMap::new();
    let (router, mut out_rx) = RouterHandle::new(map);
    let mgr = Arc::new(SessionManager::new(Duration::from_secs(30)));
    let key = SessionKey {
        chat_id: "oc_race".into(),
        thread_id: None,
    };

    // Text 1 -> SpawnAcp.
    router
        .dispatch(FeishuIn::Text {
            key: key.clone(),
            text: "msg1".into(),
            reply_to: None,
        })
        .await;
    let out = tokio::time::timeout(Duration::from_millis(500), out_rx.recv())
        .await
        .unwrap()
        .unwrap();
    let Out::SpawnAcp { key: k, prompt } = out else {
        panic!("expected SpawnAcp, got {out:?}")
    };

    // Start the (slow) spawn in the background.
    let mgr2 = mgr.clone();
    let router2 = router.clone();
    let journal_arg = journal.to_str().unwrap().to_string();
    let spawn = tokio::spawn(async move {
        sebas::run::acp_spawn_and_activate(
            &mgr2,
            &router2,
            &k,
            &prompt,
            fake().to_str().unwrap(),
            vec![
                "--journal".into(),
                journal_arg,
                "--delay-new-ms".into(),
                "500".into(),
            ],
            None,
        )
        .await
    });

    // Text 2 arrives while session/new is sleeping -> queued, no second spawn.
    router
        .dispatch(FeishuIn::Text {
            key: key.clone(),
            text: "msg2".into(),
            reply_to: None,
        })
        .await;

    let (session_id, pending) = spawn.await.unwrap().expect("spawn ok");
    assert_eq!(pending, vec!["msg2".to_string()]);
    sebas::run::flush_pending_prompts(&mgr, &session_id, pending)
        .await
        .expect("flush");

    // Drain the session's events so the child finishes both turns.
    let rx = mgr.event_rx(&session_id).await.expect("event rx");
    let mut finished = 0;
    let guard = tokio::time::timeout(Duration::from_secs(5), async {
        let mut rx = rx.lock().await;
        while finished < 2 {
            match rx.recv().await {
                Some(acp_claude::session::AcpEvent::Finished { .. }) => finished += 1,
                Some(_) => {}
                None => break,
            }
        }
    })
    .await;
    assert!(guard.is_ok() && finished == 2, "both turns should complete");

    // Journal: exactly one session/new; two session/prompt; second prompt
    // carries msg2.
    let raw = std::fs::read_to_string(&journal).expect("journal");
    let news = raw.matches("\"session/new\"").count();
    assert_eq!(news, 1, "expected exactly one session/new: {raw}");
    let prompts = raw.matches("\"session/prompt\"").count();
    assert_eq!(prompts, 2, "expected two session/prompt calls: {raw}");
    assert!(
        raw.contains("msg2"),
        "pending prompt must reach the agent: {raw}"
    );
    let _ = std::fs::remove_file(&journal);
}
```

注意：`fake()` 在 sebas crate 的测试中 `CARGO_MANIFEST_DIR` 就是 workspace 根，不需要 `.parent()`（与 acp-claude 测试不同）。

- [ ] **Step 3: 运行并确认通过**

Run: `cargo test --test spawn_race_test -- --nocapture 2>&1 | tail -8`
Expected: PASS（1 次 session/new、2 次 session/prompt、pending 拼接到达）。

若在 Task 11 之前的代码上构造等价场景，第二条消息会产生第二个 `SpawnAcp`（journal 中 session/new = 2）——此即被修复的竞态。

- [ ] **Step 4: 全量回归 + commit 3**

Run: `cargo test --workspace 2>&1 | grep -E "test result|error"`
Expected: 全部 ok。

Run: `cargo test --workspace -- --ignored --nocapture 2>&1 | grep -E "test result"`
Expected: sigterm 测试通过。

```bash
git add router/src/state.rs router/src/router.rs feishu/src/cards.rs src/run.rs \
  router/tests/spawn_race_test.rs tests/spawn_race_test.rs \
  router/tests/state_test.rs router/tests/router_test.rs \
  router/tests/permission_test.rs router/tests/terminal_error_test.rs
git commit -m "fix(router): eliminate double-spawn race with Spawning placeholder"
```

```bash
bd update sebas-jq0 --status=in_progress 2>/dev/null; bd close sebas-jq0 --reason="组3完成：MappingState 占位+pending 队列+activate/fail_spawn+dump 过滤，DELAY_NEW 集成测试确定性复现，测试全绿"
```

---

## Task 13: 收尾 — README + 最终回归

**Files:**
- Modify: `README.md`（Known limitations 段）

- [ ] **Step 1: 更新 README**

`README.md` 的 `## Known limitations` 整段替换为：

```markdown
## Known limitations

- SessionMap is in-memory only; after a daemon restart the first message per chat lazily fails over — full lazy-resume (spec §3.3e) is tracked as follow-up work.
- Slash commands `/sessions`, `/switch`, `/resume`, `/model`, `/cd`, `/status`, `/help` are parsed but not yet implemented end-to-end (fall through to a log-only HelpText); `/compact`, `/cost`, `/cancel` work.
- Media messages (image/file/audio) are recognized but files are not downloaded yet.
- Card streaming currently re-renders the card per event (history is replaced, not accumulated); thinking/tool-progress events are not surfaced.
- No coverage thresholds enforced in CI; there is no CI yet.
- The `record` subcommand for fixture capture (§4.4 of the spec) is not implemented.
```

（同时把 `## Status` 段里"handshake not verified end-to-end"一句保留——真机仍未验证。）

- [ ] **Step 2: 最终回归**

Run: `cargo test --workspace 2>&1 | grep -E "test result"`
Run: `cargo test --workspace -- --ignored --nocapture 2>&1 | grep -E "test result"`
Run: `cargo clippy --workspace --all-targets 2>&1 | grep -c "^warning"`（记录数字，目标：不高于计划执行前）
Expected: 全部 ok；无新增 warning。

- [ ] **Step 3: Commit + 汇报**

```bash
git add README.md
git commit -m "docs(readme): drop resolved known-limitations after P0 fixes"
git log --oneline -5
```

按 conservative profile 向用户汇报：分支 `fix/p0-critical-chain` 上的 4 个 commit、测试状态、bd 三个 P0 issue 已关闭；`git push` 与合并由用户决定。

---

## Self-Review 记录

- **Spec 覆盖**：D1→Task 2、D2/D3→Task 4、D4→Task 3、D5→Task 6、D6→Task 7/8/9、D7→Task 10、D8→Task 11/12；fake-claude 重设计（§6）→Task 1；错误处理矩阵（§7）→各 Task 实现点；测试矩阵（§8）12 行→Tasks 2,3,4,5,6,8,9,10,11,12 全覆盖；交付分组（§9）→commit 1/2/3 落点 Task 5/10/12。
- **有意偏离 spec 的点**：①fake 开关 env→argv（Global Constraints 已记）；②TokenManager 重试对**任何**业务 code != 0 触发（设计 §4.3 原文如此）；③`post_card_with_retry`/`request_with_retry` 作为 crate 内公开测试接口暴露（Task 10 设计说明已记）。
- **类型一致性**：`MappingState`/`TextRoute`/`route_text`/`activate`/`fail_spawn`/`begin_spawn`（Task 11）与 `acp_spawn_and_activate`/`flush_pending_prompts`（Task 12）签名在定义处与使用处一致；`Error.terminal`（Task 7）与 router guarded 臂（Task 9）一致；`TokenManager` 方法名（Task 10）与 run.rs 调用点一致。
- **自查修正记录**：初稿遗漏 `RouterHandle::{activate, fail_spawn}` 透传（run.rs 无法触达私有 `map` 字段），已补入 Task 11 Step 4 与 Produces 清单；`with_stub_token` 由 try_lock 改写为直接构造。
- **已知风险**：Task 8 的 macro_rules 宏在 run_main 内的精确落点需以编译为准微调；Task 11 旧测试迁移中若有断言依赖 `last_active_unix` 精确值需按编译错误逐个处理（Step 7 已注明）。
