/**
 * Brand copy shared across every shell surface (workbench-rail-polish D3).
 * The tagline renders in three places — the app-shell rail brand, the login
 * view and the first-run setup view — one constant, three references, no
 * drift (the former "agent router" literal had already drifted out of tune
 * with what the product is).
 */

export const APP_TAGLINE = '你忠诚的 AI 伙伴'
