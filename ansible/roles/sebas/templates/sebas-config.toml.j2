[watchdog.storage]
data_dir = "{{ sebas_data_dir }}"

[watchdog.webui]
host = "{{ sebas_webui_host }}"
port = {{ sebas_webui_port }}

[acp.claude]
path = "{{ sebas_acp_claude_path }}"

[feishu]
enabled = false
