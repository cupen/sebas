# 默认 inventory：本机部署（connection=local，不走 SSH）。
# 既作部署参考，也作本地测试环境——`ansible-playbook site.yml` 即部署到本机。
# 部署远端主机时用 `-i` 指定自己的 inventory，playbook 无需任何改动，例如：
#   [sebas]
#   prod01 ansible_host=10.0.0.8 ansible_user=admin
[sebas]
localhost ansible_connection=local ansible_python_interpreter=auto_silent
